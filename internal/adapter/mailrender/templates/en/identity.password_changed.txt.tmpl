Hello{{if .Name}} {{.Name}}{{end}},

The password on your Chronos account was {{if .ViaReset}}reset{{else}}changed{{end}} on {{datetime .OccurredAt}}{{if .Location}} from {{.Location}}{{end}}{{if .Device}}, using {{.Device}}{{end}}.
{{if .ViaReset}}
It was changed through a reset link rather than by someone who knew the old
password, and every session on the account has been signed out.
{{end}}
If this was you, nothing further is needed.
{{if .RevokeURL}}
If it wasn't, use this link to sign out every session and require a new password:

  {{.RevokeURL}}

It does not sign you in.
{{else}}
If it wasn't, somebody else has control of this account. Sign in and change the
password again straight away:

  {{url "/settings/security"}}
{{end}}
