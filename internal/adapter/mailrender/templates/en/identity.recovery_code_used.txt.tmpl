Hello{{if .Name}} {{.Name}}{{end}},

One of your recovery codes was used to sign in to Chronos on
{{datetime .OccurredAt}}. That code is now spent; you have {{.Remaining}} left.
{{if .Low}}
You are running low. Generate a new set before you run out — an account with no
codes and no authenticator is an account you cannot get back into:

  {{url "/settings/security"}}
{{end}}
A recovery code is normally used because a device was lost. If you still have
your authenticator and did not do this, somebody else has your codes. Sign in,
regenerate them, and check your second factor:

  {{url "/settings/security"}}
