# secrets / local config
.env
*.local

# go
/bin/
/dist/
*.test
*.out
/vendor/

# editors
.idea/
.vscode/
.DS_Store

# local code index (rebuildable via `codegraph index`)
/.codegraph/

# local data escaping a bind mount
/data/
bin/

# Stray `go build -o api ./cmd/api` output. The binaries are built into ./bin
# or run with `go run`; a 50 MB executable at the repo root is always an
# accident, and it shows up in the gate as an untracked file.
/api
