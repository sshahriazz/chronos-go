// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: chronos/identity/v1/identity.proto

// Package identity is the wire contract for accounts, credentials, sessions and
// the security-settings screen.
//
// Three properties of this schema are load-bearing, and each of them is a rule
// the messages below are shaped by rather than a convention they happen to
// follow.
//
// **Nothing personal is here.** Identity holds the most personal data in the
// system and puts the least of it in the log (ADR-002). Every account is named
// on the wire by its `subject_id` pseudonym, and an address appears in exactly
// three places: `RegisterRequest.email`,
// `ResendEmailVerificationRequest.email` and `AuthenticateRequest.identifier`,
// where the user has just typed it. No response returns one. That is what makes
// erasure the destruction of a key rather than a migration across every
// projection that ever copied an address.
//
// **A password is set by the party that proved the mailbox, and by nobody
// else.** `RegisterRequest` carries NO password and field 2 is reserved so it
// can never carry one again; `VerifyEmailRequest` carries it instead. The
// ordering is the whole defence against the pre-hijacking attack (Sudhodanan &
// Paverd, USENIX Security 2022): a credential set at registration is set by
// whoever typed the address, and the verification click that follows proves
// control of the MAILBOX rather than authorship of that credential. With no
// credential in existence until the link is followed, there is nothing for a
// stranger's registration to leave behind.
//
// **A secret crosses this boundary at most once.** The session token, the TOTP
// shared secret and its provisioning URI, and the plaintext recovery codes are
// returned by the single call that mints them and are then unrecoverable. There
// is deliberately no endpoint that re-displays any of them: one that did would
// turn a stolen session into a permanent bypass of every factor on the account.
// No password, verifier, digest, sealed secret or blind index appears anywhere
// in this file, in either direction.
//
// **A refusal says nothing (ADR-036).** An unknown identifier, a wrong password,
// a wrong code, a replayed code, an unverified address, a deactivated account, a
// suspended account and a throttled attempt all produce one undifferentiated
// error. No message here carries a field that could distinguish them. The real
// reason is recorded in the event log, where an operator can read it and an
// attacker cannot, and it is surfaced to the account HOLDER — after the fact,
// about their own account — by ListLoginHistory.
//
// **Validation here is deliberately asymmetric, and the asymmetry is the third
// property enforced rather than described.** Constraints are declarative
// protovalidate rules (ADR-007, CONVENTIONS §7) and the interceptor applies them
// before a handler runs — so on the ENROLMENT side, where the rules are public
// and printed next to the signup form, they are stated in full: RegisterRequest
// carries the whole address and password policy.
//
// On the AUTHENTICATION side they are almost entirely absent, and their absence
// is load-bearing. A request refused as malformed tells the caller their guess
// was structurally wrong, which is information a wrong password does not give.
// AuthenticateRequest.password, AuthenticateRequest.code,
// CreateSessionRequest.password and CreateSessionRequest.code therefore carry NO
// rules at all: no length, no shape, no `required`. A validation rule on a
// credential being presented is an oracle, and ADR-036's single undifferentiated
// refusal cannot survive one.
package identityv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/chronos/chronos-go/gen/proto/chronos/identity/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// IdentityServiceName is the fully-qualified name of the IdentityService service.
	IdentityServiceName = "chronos.identity.v1.IdentityService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// IdentityServiceRegisterProcedure is the fully-qualified name of the IdentityService's Register
	// RPC.
	IdentityServiceRegisterProcedure = "/chronos.identity.v1.IdentityService/Register"
	// IdentityServiceVerifyEmailProcedure is the fully-qualified name of the IdentityService's
	// VerifyEmail RPC.
	IdentityServiceVerifyEmailProcedure = "/chronos.identity.v1.IdentityService/VerifyEmail"
	// IdentityServiceResendEmailVerificationProcedure is the fully-qualified name of the
	// IdentityService's ResendEmailVerification RPC.
	IdentityServiceResendEmailVerificationProcedure = "/chronos.identity.v1.IdentityService/ResendEmailVerification"
	// IdentityServiceCheckUsernameAvailabilityProcedure is the fully-qualified name of the
	// IdentityService's CheckUsernameAvailability RPC.
	IdentityServiceCheckUsernameAvailabilityProcedure = "/chronos.identity.v1.IdentityService/CheckUsernameAvailability"
	// IdentityServiceRequestPasswordResetProcedure is the fully-qualified name of the IdentityService's
	// RequestPasswordReset RPC.
	IdentityServiceRequestPasswordResetProcedure = "/chronos.identity.v1.IdentityService/RequestPasswordReset"
	// IdentityServiceResetPasswordProcedure is the fully-qualified name of the IdentityService's
	// ResetPassword RPC.
	IdentityServiceResetPasswordProcedure = "/chronos.identity.v1.IdentityService/ResetPassword"
	// IdentityServiceAuthenticateProcedure is the fully-qualified name of the IdentityService's
	// Authenticate RPC.
	IdentityServiceAuthenticateProcedure = "/chronos.identity.v1.IdentityService/Authenticate"
	// IdentityServiceCreateSessionProcedure is the fully-qualified name of the IdentityService's
	// CreateSession RPC.
	IdentityServiceCreateSessionProcedure = "/chronos.identity.v1.IdentityService/CreateSession"
	// IdentityServiceGetUserProcedure is the fully-qualified name of the IdentityService's GetUser RPC.
	IdentityServiceGetUserProcedure = "/chronos.identity.v1.IdentityService/GetUser"
	// IdentityServiceListSessionsProcedure is the fully-qualified name of the IdentityService's
	// ListSessions RPC.
	IdentityServiceListSessionsProcedure = "/chronos.identity.v1.IdentityService/ListSessions"
	// IdentityServiceListMethodsProcedure is the fully-qualified name of the IdentityService's
	// ListMethods RPC.
	IdentityServiceListMethodsProcedure = "/chronos.identity.v1.IdentityService/ListMethods"
	// IdentityServiceListLoginHistoryProcedure is the fully-qualified name of the IdentityService's
	// ListLoginHistory RPC.
	IdentityServiceListLoginHistoryProcedure = "/chronos.identity.v1.IdentityService/ListLoginHistory"
	// IdentityServiceEnrollTotpProcedure is the fully-qualified name of the IdentityService's
	// EnrollTotp RPC.
	IdentityServiceEnrollTotpProcedure = "/chronos.identity.v1.IdentityService/EnrollTotp"
	// IdentityServiceConfirmTotpProcedure is the fully-qualified name of the IdentityService's
	// ConfirmTotp RPC.
	IdentityServiceConfirmTotpProcedure = "/chronos.identity.v1.IdentityService/ConfirmTotp"
	// IdentityServiceGenerateRecoveryCodesProcedure is the fully-qualified name of the
	// IdentityService's GenerateRecoveryCodes RPC.
	IdentityServiceGenerateRecoveryCodesProcedure = "/chronos.identity.v1.IdentityService/GenerateRecoveryCodes"
	// IdentityServiceRevokeSessionProcedure is the fully-qualified name of the IdentityService's
	// RevokeSession RPC.
	IdentityServiceRevokeSessionProcedure = "/chronos.identity.v1.IdentityService/RevokeSession"
	// IdentityServiceRevokeAllSessionsProcedure is the fully-qualified name of the IdentityService's
	// RevokeAllSessions RPC.
	IdentityServiceRevokeAllSessionsProcedure = "/chronos.identity.v1.IdentityService/RevokeAllSessions"
	// IdentityServiceDeactivateAccountProcedure is the fully-qualified name of the IdentityService's
	// DeactivateAccount RPC.
	IdentityServiceDeactivateAccountProcedure = "/chronos.identity.v1.IdentityService/DeactivateAccount"
	// IdentityServiceRequestAccountDeletionProcedure is the fully-qualified name of the
	// IdentityService's RequestAccountDeletion RPC.
	IdentityServiceRequestAccountDeletionProcedure = "/chronos.identity.v1.IdentityService/RequestAccountDeletion"
	// IdentityServiceCancelAccountDeletionProcedure is the fully-qualified name of the
	// IdentityService's CancelAccountDeletion RPC.
	IdentityServiceCancelAccountDeletionProcedure = "/chronos.identity.v1.IdentityService/CancelAccountDeletion"
	// IdentityServiceRequestEmailChangeProcedure is the fully-qualified name of the IdentityService's
	// RequestEmailChange RPC.
	IdentityServiceRequestEmailChangeProcedure = "/chronos.identity.v1.IdentityService/RequestEmailChange"
	// IdentityServiceCancelEmailChangeProcedure is the fully-qualified name of the IdentityService's
	// CancelEmailChange RPC.
	IdentityServiceCancelEmailChangeProcedure = "/chronos.identity.v1.IdentityService/CancelEmailChange"
	// IdentityServiceConfirmEmailChangeProcedure is the fully-qualified name of the IdentityService's
	// ConfirmEmailChange RPC.
	IdentityServiceConfirmEmailChangeProcedure = "/chronos.identity.v1.IdentityService/ConfirmEmailChange"
	// IdentityServiceRevertEmailChangeProcedure is the fully-qualified name of the IdentityService's
	// RevertEmailChange RPC.
	IdentityServiceRevertEmailChangeProcedure = "/chronos.identity.v1.IdentityService/RevertEmailChange"
	// IdentityServiceBeginPasskeyRegistrationProcedure is the fully-qualified name of the
	// IdentityService's BeginPasskeyRegistration RPC.
	IdentityServiceBeginPasskeyRegistrationProcedure = "/chronos.identity.v1.IdentityService/BeginPasskeyRegistration"
	// IdentityServiceFinishPasskeyRegistrationProcedure is the fully-qualified name of the
	// IdentityService's FinishPasskeyRegistration RPC.
	IdentityServiceFinishPasskeyRegistrationProcedure = "/chronos.identity.v1.IdentityService/FinishPasskeyRegistration"
	// IdentityServiceBeginPasskeyLoginProcedure is the fully-qualified name of the IdentityService's
	// BeginPasskeyLogin RPC.
	IdentityServiceBeginPasskeyLoginProcedure = "/chronos.identity.v1.IdentityService/BeginPasskeyLogin"
	// IdentityServiceFinishPasskeyLoginProcedure is the fully-qualified name of the IdentityService's
	// FinishPasskeyLogin RPC.
	IdentityServiceFinishPasskeyLoginProcedure = "/chronos.identity.v1.IdentityService/FinishPasskeyLogin"
	// IdentityServiceListPasskeysProcedure is the fully-qualified name of the IdentityService's
	// ListPasskeys RPC.
	IdentityServiceListPasskeysProcedure = "/chronos.identity.v1.IdentityService/ListPasskeys"
	// IdentityServiceRemovePasskeyProcedure is the fully-qualified name of the IdentityService's
	// RemovePasskey RPC.
	IdentityServiceRemovePasskeyProcedure = "/chronos.identity.v1.IdentityService/RemovePasskey"
	// IdentityServiceListFederatedProvidersProcedure is the fully-qualified name of the
	// IdentityService's ListFederatedProviders RPC.
	IdentityServiceListFederatedProvidersProcedure = "/chronos.identity.v1.IdentityService/ListFederatedProviders"
	// IdentityServiceBeginFederatedSignInProcedure is the fully-qualified name of the IdentityService's
	// BeginFederatedSignIn RPC.
	IdentityServiceBeginFederatedSignInProcedure = "/chronos.identity.v1.IdentityService/BeginFederatedSignIn"
	// IdentityServiceFinishFederatedSignInProcedure is the fully-qualified name of the
	// IdentityService's FinishFederatedSignIn RPC.
	IdentityServiceFinishFederatedSignInProcedure = "/chronos.identity.v1.IdentityService/FinishFederatedSignIn"
	// IdentityServiceBeginFederatedLinkProcedure is the fully-qualified name of the IdentityService's
	// BeginFederatedLink RPC.
	IdentityServiceBeginFederatedLinkProcedure = "/chronos.identity.v1.IdentityService/BeginFederatedLink"
	// IdentityServiceFinishFederatedLinkProcedure is the fully-qualified name of the IdentityService's
	// FinishFederatedLink RPC.
	IdentityServiceFinishFederatedLinkProcedure = "/chronos.identity.v1.IdentityService/FinishFederatedLink"
	// IdentityServiceUnlinkFederatedIdentityProcedure is the fully-qualified name of the
	// IdentityService's UnlinkFederatedIdentity RPC.
	IdentityServiceUnlinkFederatedIdentityProcedure = "/chronos.identity.v1.IdentityService/UnlinkFederatedIdentity"
	// IdentityServiceCreateServiceAccountProcedure is the fully-qualified name of the IdentityService's
	// CreateServiceAccount RPC.
	IdentityServiceCreateServiceAccountProcedure = "/chronos.identity.v1.IdentityService/CreateServiceAccount"
	// IdentityServiceListServiceAccountsProcedure is the fully-qualified name of the IdentityService's
	// ListServiceAccounts RPC.
	IdentityServiceListServiceAccountsProcedure = "/chronos.identity.v1.IdentityService/ListServiceAccounts"
	// IdentityServiceCreateApiKeyProcedure is the fully-qualified name of the IdentityService's
	// CreateApiKey RPC.
	IdentityServiceCreateApiKeyProcedure = "/chronos.identity.v1.IdentityService/CreateApiKey"
	// IdentityServiceRotateApiKeyProcedure is the fully-qualified name of the IdentityService's
	// RotateApiKey RPC.
	IdentityServiceRotateApiKeyProcedure = "/chronos.identity.v1.IdentityService/RotateApiKey"
	// IdentityServiceRevokeApiKeyProcedure is the fully-qualified name of the IdentityService's
	// RevokeApiKey RPC.
	IdentityServiceRevokeApiKeyProcedure = "/chronos.identity.v1.IdentityService/RevokeApiKey"
	// IdentityServiceListApiKeysProcedure is the fully-qualified name of the IdentityService's
	// ListApiKeys RPC.
	IdentityServiceListApiKeysProcedure = "/chronos.identity.v1.IdentityService/ListApiKeys"
)

// IdentityServiceClient is a client for the chronos.identity.v1.IdentityService service.
type IdentityServiceClient interface {
	// Register claims an address and creates the account that claims it.
	//
	// Public: the caller has no session and, by definition, no account. It is a
	// WRITE rather than a GROW — a person is not a seat, and seats are consumed
	// when an organization adds a member, not when a human being signs up.
	//
	// It sets NO PASSWORD and creates no credential of any kind. The account is
	// unusable by everybody until the link mailed to the address is followed and
	// VerifyEmail supplies one.
	//
	// The response is identical whether or not the address was free. See
	// RegisterResponse.
	Register(context.Context, *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error)
	// VerifyEmail proves control of an address, sets the account's first
	// password, and makes the claim on the address permanent.
	//
	// Public: it is reached from a link in a mailbox, by a browser that has never
	// authenticated. The token IS the authentication, and it is single-use.
	//
	// It is the ONLY call that can give a brand-new account a password, and that
	// is the pre-hijacking defence: the credential is created by the party that
	// has just proven the mailbox, in the same request as the proof.
	VerifyEmail(context.Context, *connect.Request[v1.VerifyEmailRequest]) (*connect.Response[v1.VerifyEmailResponse], error)
	// ResendEmailVerification issues a new verification link for an address whose
	// account has not proven it yet.
	//
	// Public, and the choice is forced rather than preferred. A Pending account
	// can hold no session, so requiring one would make this call unavailable to
	// every account that needs it — the endpoint would exist and serve nobody.
	//
	// The cost of being public is that it is an unauthenticated mail trigger
	// pointed at a third party's mailbox, so it is rate limited on TWO axes before
	// anything is looked up: per address, which bounds mail-bombing one victim, and
	// per caller, which bounds an enumeration sweep. Both counters are consumed
	// whether or not an account exists, so the ceiling itself is not an oracle.
	//
	// A WRITE: the successful path appends EmailVerificationRequested to the
	// account's stream, and the verification reactor does the rest.
	ResendEmailVerification(context.Context, *connect.Request[v1.ResendEmailVerificationRequest]) (*connect.Response[v1.ResendEmailVerificationResponse], error)
	// CheckUsernameAvailability reports whether a public handle can be claimed.
	//
	// Public: it is reached from the signup form, by a browser that has never
	// authenticated and by definition has no account.
	//
	// # It IS an enumeration oracle, and that is the intended behaviour
	//
	// A caller can learn that @alice is taken. That is not a leak, because a handle
	// is PUBLISHED — publication is its entire purpose (ADR-051) — and the same
	// fact is readable from any page that renders a mention, from a profile URL,
	// and from the person themselves. Every other public RPC in this service is
	// shaped to make its two outcomes indistinguishable; this one is shaped to make
	// them distinguishable, and the difference is not an oversight in the schema.
	//
	// Do NOT "fix" this into an undifferentiated response. The resulting endpoint
	// would answer no question at all, the signup form could not tell a person
	// their handle was taken until after they had spent a verification link they
	// cannot get back, and the information would remain freely available everywhere
	// else in the product.
	//
	// The ONE thing it must not distinguish is a tombstoned handle from a taken
	// one; see CheckUsernameAvailabilityResponse for why that single merge is a
	// privacy control rather than the same tidiness.
	//
	// # The answer is advisory
	//
	// It reads the handle's reservation stream at an instant. Uniqueness is
	// enforced at the moment of the WRITE by an atomic append with a precondition
	// (ADR-044, ADR-051), so a handle reported free may be taken by the time
	// VerifyEmail claims it — any check-then-claim is racy by construction. The
	// point of this call is that a person finds out at the FORM rather than after
	// clicking a link.
	//
	// A READ: it appends nothing, mints nothing and sends no mail. It is rate
	// limited per caller regardless — "an attacker may learn whether @alice exists"
	// is not the same permission as "an attacker may read a stream from the event
	// store as fast as they can open sockets".
	CheckUsernameAvailability(context.Context, *connect.Request[v1.CheckUsernameAvailabilityRequest]) (*connect.Response[v1.CheckUsernameAvailabilityResponse], error)
	// RequestPasswordReset sends a reset link to the address a registered account
	// holds.
	//
	// Public, and the choice is forced rather than preferred: the population that
	// needs this call is exactly the population that cannot sign in.
	//
	// The cost of being public is that it is an unauthenticated mail trigger
	// pointed at a third party's mailbox, so it is rate limited on TWO axes before
	// anything is looked up — per address, which bounds mail-bombing one victim,
	// and per caller, which bounds an enumeration sweep. Both counters are
	// consumed whether or not an account exists, so the ceiling itself is not an
	// oracle. The per-address counter is the SAME one verification mail spends
	// (NOTIFICATIONS.md §4: "an hourly ceiling per address across all classes"), so
	// an attacker cannot double a victim's mail by alternating between the two
	// endpoints.
	//
	// A WRITE: the successful path appends PasswordResetRequested to the account's
	// stream, and the reset-mail issuer does the rest.
	RequestPasswordReset(context.Context, *connect.Request[v1.RequestPasswordResetRequest]) (*connect.Response[v1.RequestPasswordResetResponse], error)
	// ResetPassword redeems a reset link and replaces the account's password.
	//
	// Public: it is reached from a link in a mailbox, by a browser that has never
	// authenticated. The token IS the authentication, and it is single-use.
	//
	// It grants NOTHING beyond the credential change. No session is created, no
	// bearer token is returned, the second factor is untouched, and a Pending
	// account is not activated — so a reset cannot be used to bypass the second
	// factor (ASVS 5.0 V6.4.3). The caller's next act is an ordinary
	// CreateSession.
	//
	// In the same request it voids every session for the account including any
	// held by whoever is resetting, every outstanding token of every purpose, and
	// any pending identifier change (identity.md §4.4, §4.5).
	ResetPassword(context.Context, *connect.Request[v1.ResetPasswordRequest]) (*connect.Response[v1.ResetPasswordResponse], error)
	// Authenticate verifies the factors presented and reports what remains
	// (ADR-050).
	//
	// Public: this is where a session comes from, so requiring one is circular.
	//
	// A WRITE, and not a read despite minting nothing: every attempt appends to
	// the authentication journal, and classing it as a read would let it through
	// gates whose whole purpose is to stop writes. There is no `min_aal` for the
	// same reason there is no authz — the caller has no session to have a level.
	Authenticate(context.Context, *connect.Request[v1.AuthenticateRequest]) (*connect.Response[v1.AuthenticateResponse], error)
	// CreateSession completes a login and mints the bearer token.
	//
	// Public, for the same reason as Authenticate: it is the call that creates the
	// session everything else requires.
	CreateSession(context.Context, *connect.Request[v1.CreateSessionRequest]) (*connect.Response[v1.CreateSessionResponse], error)
	// GetUser returns the caller's own account: what it is, where it is in its
	// lifecycle, and whether its address has been proven.
	//
	// AAL1 — no `min_aal` declared, so the default applies. This discloses nothing
	// an attacker holding a session does not already have, and demanding step-up
	// to render the account screen would make step-up routine, which is how people
	// learn to approve it without reading it.
	GetUser(context.Context, *connect.Request[v1.GetUserRequest]) (*connect.Response[v1.GetUserResponse], error)
	// ListSessions returns the caller's device list, newest first, one page at a
	// time.
	//
	// AAL1. It is a read of a list the holder needs in order to NOTICE a
	// compromise, and putting step-up in front of noticing is backwards — the
	// revocation that follows is what carries the AAL2 requirement.
	ListSessions(context.Context, *connect.Request[v1.ListSessionsRequest]) (*connect.Response[v1.ListSessionsResponse], error)
	// ListMethods returns the authentication methods on the caller's account:
	// which exist, what kind each is, whether it can be used now, and when it last
	// was.
	//
	// AAL1, on the same argument as ListSessions: this is the screen on which
	// somebody discovers an authenticator they did not enrol.
	ListMethods(context.Context, *connect.Request[v1.ListMethodsRequest]) (*connect.Response[v1.ListMethodsResponse], error)
	// ListLoginHistory returns recent authentication attempts against the caller's
	// account, successes and failures alike, newest first.
	//
	// AAL1, and deliberately so even though this is the most sensitive read here.
	// It is the account holder's own security log, and it is only useful if they
	// actually look at it.
	ListLoginHistory(context.Context, *connect.Request[v1.ListLoginHistoryRequest]) (*connect.Response[v1.ListLoginHistoryResponse], error)
	// EnrollTotp provisions a new authenticator secret and returns it once.
	//
	// AAL2. Enrolling a second factor is how an attacker holding one factor makes
	// their access durable, so it must not be reachable from a session that only
	// ever presented one. It replaces any unproven enrolment on the account, so
	// the abandoned secret stops existing at the moment the new one is written
	// rather than lingering as a second confirmable factor.
	//
	// AAL1 while the account has never held a proven second factor, and only
	// then. Read the two levels together and they say: presenting the existing
	// factor is what buys the right to add another, and an account with no
	// existing factor is exempt from a demand nothing could satisfy. Without the
	// exemption an account registered through this API could never activate — the
	// factor it must have to reach AAL2 is the factor this call exists to give it
	// (identity.md §2).
	//
	// The exemption is not a weaker version of the same permission. It applies to
	// a strictly poorer target: an account with no second factor, whose password
	// is therefore already the only thing standing between an attacker and it. An
	// attacker who enrols there has gained nothing they did not already have. On
	// an account that HAS a factor the floor is AAL2, so the same attacker must
	// present that factor — and it does not re-open when a factor is removed,
	// because the window closes on first proof and never re-opens.
	EnrollTotp(context.Context, *connect.Request[v1.EnrollTotpRequest]) (*connect.Response[v1.EnrollTotpResponse], error)
	// ConfirmTotp proves possession of a provisioned secret, and may be what
	// activates the account.
	//
	// AAL2, matching EnrollTotp: a confirmation is the half of an enrolment that
	// makes a factor real, and letting it run at a lower level than the enrolment
	// itself would leave the pair only as strong as its weaker call.
	//
	// The bootstrap floor matches EnrollTotp for the same reason. The two calls
	// are one operation split in half, and exempting only the first would leave
	// an account able to provision a secret it could never prove — the deadlock
	// moved one call to the right rather than removed.
	//
	// This is the call that CLOSES the window: the account it activates has a
	// proven second factor from that moment, so the next call to either method
	// faces AAL2. The exemption is therefore self-limiting, and it is limited by
	// the very thing it was granted to produce.
	ConfirmTotp(context.Context, *connect.Request[v1.ConfirmTotpRequest]) (*connect.Response[v1.ConfirmTotpResponse], error)
	// GenerateRecoveryCodes replaces the whole recovery-code set and returns the
	// new plaintext codes once.
	//
	// AAL2. A recovery code is a standing bypass of every other factor, so minting
	// a fresh set from a single-factor session would hand an attacker a permanent
	// credential that survives the password change the victim makes in response.
	// Generating codes never activates an account — only a real second factor
	// does.
	//
	// NO bootstrap exemption, deliberately, and this is the method where the
	// decision is load-bearing. The exemption exists to break a deadlock, and
	// there is no deadlock here: recovery codes cannot activate an account, so
	// nothing about the path from registered to Active runs through this call. An
	// account reaches its first AAL2 session by confirming a TOTP secret, and only
	// then asks for codes — by which time the strict floor is satisfiable. Granting
	// the exemption anyway would mean a single-factor session could mint the one
	// credential that outlives every remedy the account holder has, on an account
	// that has no second factor to lose. That is the exact hole the floor is
	// shaped to keep shut, and it would be opened for no gain.
	GenerateRecoveryCodes(context.Context, *connect.Request[v1.GenerateRecoveryCodesRequest]) (*connect.Response[v1.GenerateRecoveryCodesResponse], error)
	// RevokeSession ends one of the caller's sessions.
	//
	// AAL2. Revocation is the control a person reaches for when they believe they
	// are compromised, and it must not be usable BY the compromise: a
	// single-factor session that could end sessions could sign the legitimate
	// holder out of the device they are using to investigate.
	RevokeSession(context.Context, *connect.Request[v1.RevokeSessionRequest]) (*connect.Response[v1.RevokeSessionResponse], error)
	// RevokeAllSessions ends every live session on the caller's account, in one
	// atomic append.
	//
	// AAL2, on the same argument as RevokeSession and more so: this is the
	// account-wide version, so the denial of service an attacker could inflict
	// with it is total.
	RevokeAllSessions(context.Context, *connect.Request[v1.RevokeAllSessionsRequest]) (*connect.Response[v1.RevokeAllSessionsResponse], error)
	// DeactivateAccount switches the caller's own account off, and ends every
	// session on it in the same atomic append.
	//
	// # Reversible, and how
	//
	// Deactivation is the holder's own switch and the holder undoes it by SIGNING
	// IN again with every factor the account has — the successful authentication
	// records the reactivation in the same append as itself, so the account is on
	// again before the new session exists. There is deliberately no
	// `ReactivateAccount` RPC: it would need a session, a session needs an
	// authentication, and an authentication refuses a deactivated account, so its
	// only precondition would be one its own subject can never satisfy. See
	// `identity.md` §1.
	//
	// Suspension is the administrative counterpart and is NOT here. It is not
	// reversible by the holder, every method on this service is reached by the
	// holder acting on their own account, and there is no operator surface in this
	// repository yet — so an RPC for it could only be a self-suspension, which
	// makes the account unreachable by every route this module has.
	//
	// # AAL2
	//
	// On RevokeAllSessions' argument: the sign-out is total, so the denial of
	// service an attacker holding a stolen password could inflict with it is total.
	// No `bootstrap_min_aal`, deliberately — an account still enrolling its first
	// factor has nothing to switch off, and the exemption exists for enrolment
	// alone.
	DeactivateAccount(context.Context, *connect.Request[v1.DeactivateAccountRequest]) (*connect.Response[v1.DeactivateAccountResponse], error)
	// RequestAccountDeletion asks for the caller's own account to be erased, and
	// starts the grace period.
	//
	// # The account keeps working until the deadline
	//
	// A request is not a deletion. `compliance` runs the grace period as a durable
	// workflow and erases at the end of it; until then everything works normally,
	// and `CancelAccountDeletion` withdraws the request.
	//
	// This is stated on the RPC rather than only in a design document because a
	// client that reads "deletion requested" as "account gone" will build a screen
	// that lies to the person using it — and the screen that matters most here is
	// the one offering the way back.
	//
	// # AAL2, plus a typed confirmation
	//
	// identity.md §2 lists account deletion among the operations that require
	// step-up. The step-up proves WHO is asking; the `confirmation` field is what
	// makes it hard to ask by ACCIDENT — a distinct property, and the one
	// cancellation cannot cover, because cancelling requires somebody to notice
	// and the person who never meant to ask is the least likely to.
	//
	// # It does not sign anybody out
	//
	// Every other destructive call here voids sessions. This one deliberately does
	// not: the grace period exists so the person can change their mind, and signing
	// them out of an account that still works teaches them the request took effect
	// immediately, which it did not.
	RequestAccountDeletion(context.Context, *connect.Request[v1.RequestAccountDeletionRequest]) (*connect.Response[v1.RequestAccountDeletionResponse], error)
	// CancelAccountDeletion withdraws the caller's own outstanding erasure request.
	//
	// # This is what makes the grace period real
	//
	// A waiting period nobody can use is a delay, not a safeguard. The window
	// exists so that somebody who clicked in anger, or whose session was taken,
	// can stop what was started — and the erasure workflow re-reads on every wake
	// precisely so that this call stops it.
	//
	// Cancelling when nothing is outstanding SUCCEEDS and records nothing. The
	// cancel link in the "deletion scheduled" mail gets clicked twice, or after an
	// operator already withdrew the request; neither person did anything wrong and
	// neither should be told they did.
	//
	// # AAL2, and no typed confirmation
	//
	// Step-up for the same reason requesting needs it: this decides whether an
	// account survives, and an attacker holding a low-assurance session must not be
	// able to keep alive an account its owner is trying to close.
	//
	// No confirmation field, and the asymmetry is deliberate. A typed confirmation
	// guards something that cannot be undone; this IS the undo, and putting a
	// hurdle in front of somebody who has just realised their mistake protects
	// nothing.
	//
	// # Why it notifies
	//
	// The catalogue mails a Security-class message on cancellation, which reads as
	// reassurance and is not only that. Somebody who deliberately asked for erasure
	// and whose request was withdrawn by SOMEBODY ELSE needs to know: that is
	// exactly what an attacker with a stolen session does when the holder tries to
	// close the account under them.
	//
	// Returns `NOT_FOUND` for an account already erased — there is nothing to come
	// back to, and saying so specifically would confirm that a particular person
	// once held it.
	CancelAccountDeletion(context.Context, *connect.Request[v1.CancelAccountDeletionRequest]) (*connect.Response[v1.CancelAccountDeletionResponse], error)
	// RequestEmailChange starts a move to a different email address.
	//
	// Nothing about the account moves here, and that is the design rather than a
	// limitation: identity.md §12 requires the NEW address to be proven first,
	// because otherwise an attacker holding a session takes ownership by naming an
	// address they cannot read. This claims the address and mails a link to it.
	//
	// Two messages go out. The link goes to the new address. A SECURITY warning
	// goes to the address the account uses today — with no link and not even the
	// new address in it, since that mailbox is exactly the one an attacker may
	// already be reading. That warning is what makes the flow survivable, and it
	// is why this RPC is worth having rather than an administrator's edit.
	//
	// AAL2, because identity.md §4.3 lists an email change among the operations
	// that require step-up. There is no `bootstrap_min_aal`: an account with no
	// second factor has no business moving its recovery address, and the bootstrap
	// exemption exists to let somebody ENROL a factor, not to widen what a
	// single-factor session may do.
	RequestEmailChange(context.Context, *connect.Request[v1.RequestEmailChangeRequest]) (*connect.Response[v1.RequestEmailChangeResponse], error)
	// CancelEmailChange calls off a pending change.
	//
	// The action the security warning tells somebody to take. It releases the
	// claim on the address and VOIDS the link already sitting in that mailbox, so
	// a change the account holder refused cannot complete afterwards.
	//
	// Idempotent: cancelling when nothing is pending is success, because the
	// caller asked for a state that already holds.
	CancelEmailChange(context.Context, *connect.Request[v1.CancelEmailChangeRequest]) (*connect.Response[v1.CancelEmailChangeResponse], error)
	// ConfirmEmailChange redeems the link mailed to the new address.
	//
	// Public: it is reached from a mailbox by a browser that has never
	// authenticated — and after this call, could not be, because completing the
	// change voids every session on the account (identity.md §4.4). The token IS
	// the authentication and it is single-use.
	//
	// It grants nothing beyond the address move. No session is created and no
	// bearer is returned; the caller's next act is an ordinary CreateSession with
	// the new address.
	//
	// The old address is NOT released. It is held for a revert window and a link
	// to undo the change is mailed to it, which is the remedy for the case where
	// the party who proved the new address was not the account's owner.
	ConfirmEmailChange(context.Context, *connect.Request[v1.ConfirmEmailChangeRequest]) (*connect.Response[v1.ConfirmEmailChangeResponse], error)
	// RevertEmailChange undoes a completed change.
	//
	// Public, and reached from a link mailed to the address the account moved AWAY
	// from — so whoever redeems it has proven control of the address the account
	// had BEFORE the change. That is the whole remedy identity.md §12 asks for: an
	// attacker holding a session can move the address, and cannot stop the real
	// owner being told and undoing it.
	//
	// It voids every session again, for a sharper reason than the change did: the
	// party this is being undone against may still be holding the session they did
	// it from.
	//
	// The window is finite. After it the old address is available to anybody, so
	// this refuses rather than taking it back from whoever claimed it since.
	RevertEmailChange(context.Context, *connect.Request[v1.RevertEmailChangeRequest]) (*connect.Response[v1.RevertEmailChangeResponse], error)
	// BeginPasskeyRegistration starts enrolling an authenticator.
	//
	// Returns the JSON for `navigator.credentials.create` and an opaque ceremony
	// id. Nothing is enrolled until FinishPasskeyRegistration verifies what the
	// authenticator signed.
	//
	// AAL2: adding a credential that can sign in on its own is a credential
	// change, and identity.md §4.3 requires step-up for those.
	BeginPasskeyRegistration(context.Context, *connect.Request[v1.BeginPasskeyRegistrationRequest]) (*connect.Response[v1.BeginPasskeyRegistrationResponse], error)
	// FinishPasskeyRegistration verifies the authenticator's answer and enrols it.
	//
	// The ceremony is SINGLE-USE: replaying one is refused, because one signature
	// must not become two credentials.
	//
	// On an account's FIRST passkey this returns recovery codes, once.
	// identity.md §5 calls lockout "the real design problem" — somebody whose only
	// method is a passkey on a lost device must still get back in — so the codes
	// are issued here rather than offered afterwards, because an afterthought is a
	// path most people never take.
	FinishPasskeyRegistration(context.Context, *connect.Request[v1.FinishPasskeyRegistrationRequest]) (*connect.Response[v1.FinishPasskeyRegistrationResponse], error)
	// BeginPasskeyLogin starts a usernameless sign-in.
	//
	// PUBLIC, and it carries NO identifier. That absence is the feature: the
	// authenticator names the account, so this endpoint cannot be asked whether an
	// address exists. A field for one would reintroduce the enumeration oracle
	// every other public surface in this module removes.
	//
	// It looks up nothing and answers identically whether or not any account
	// exists.
	BeginPasskeyLogin(context.Context, *connect.Request[v1.BeginPasskeyLoginRequest]) (*connect.Response[v1.BeginPasskeyLoginResponse], error)
	// FinishPasskeyLogin verifies an assertion and creates a session.
	//
	// Public: the assertion IS the authentication. A user-verified passkey reaches
	// AAL2 on its own (identity.md §2) — the authenticator is the possession
	// factor and the PIN or biometric that unlocked it is the second — so this can
	// mint a full session in one gesture. Without user verification it reaches
	// AAL1 and anything sensitive will ask for step-up.
	//
	// A signature counter that went BACKWARDS does NOT refuse the sign-in. Most
	// synced passkeys report a counter of zero forever, and identity.md §5 is
	// explicit that treating a regression as fatal "locks out legitimate users".
	// The session is capped at AAL1 instead and the response says so.
	FinishPasskeyLogin(context.Context, *connect.Request[v1.FinishPasskeyLoginRequest]) (*connect.Response[v1.FinishPasskeyLoginResponse], error)
	// ListPasskeys shows the caller their own enrolled authenticators.
	//
	// The security screen. It returns the label the person chose, when each was
	// added and last used, and whether one has ever produced a counter regression
	// — which is what lets somebody notice a credential they do not recognise.
	ListPasskeys(context.Context, *connect.Request[v1.ListPasskeysRequest]) (*connect.Response[v1.ListPasskeysResponse], error)
	// RemovePasskey deletes one of the caller's authenticators.
	//
	// Refused when it would leave the account with no way to sign in, which is the
	// same rule that stops the last second factor being removed. Somebody deleting
	// their only passkey from a device they no longer have is not helped by an
	// endpoint that lets them.
	//
	// AAL2 with no bootstrap exemption: removing a credential is not a way to
	// enrol one.
	RemovePasskey(context.Context, *connect.Request[v1.RemovePasskeyRequest]) (*connect.Response[v1.RemovePasskeyResponse], error)
	// ListFederatedProviders names the identity providers this deployment
	// supports.
	//
	// PUBLIC and deliberately uninteresting: it says what the DEPLOYMENT
	// supports, never what any account uses. A client needs it to render buttons,
	// and it discloses nothing about anybody.
	ListFederatedProviders(context.Context, *connect.Request[v1.ListFederatedProvidersRequest]) (*connect.Response[v1.ListFederatedProvidersResponse], error)
	// BeginFederatedSignIn starts signing in with an identity provider.
	//
	// Public: the population reaching it has not authenticated, and by definition
	// may have no account at all. It carries no identifier, so it cannot be asked
	// whether an address exists.
	//
	// A WRITE, because it records a ceremony: PKCE, state and nonce are bound to
	// this request and stored server-side, which is what makes the callback
	// verifiable rather than merely plausible.
	BeginFederatedSignIn(context.Context, *connect.Request[v1.BeginFederatedSignInRequest]) (*connect.Response[v1.BeginFederatedSignInResponse], error)
	// FinishFederatedSignIn redeems the provider's callback.
	//
	// # It does NOT always produce a session, and that is the design
	//
	// identity.md §7 forbids auto-linking a federated identity to an existing
	// account on an email match alone — the attacker registers at a provider using
	// the victim's address, the provider does not verify it, and naive matching
	// hands them the account. So a genuine sign-in whose conditions are not met
	// answers `link_refused` with no session, and the person authenticates with an
	// existing method and links explicitly from settings.
	//
	// `account_exists` rides with it so a client can say "you already have an
	// account, sign in and link this" rather than the dead end §7.5 names, where a
	// second account is created silently and one person owns two.
	//
	// The session it does create is AAL1: a provider may have required its own
	// second factor and this system cannot know that it did.
	FinishFederatedSignIn(context.Context, *connect.Request[v1.FinishFederatedSignInRequest]) (*connect.Response[v1.FinishFederatedSignInResponse], error)
	// BeginFederatedLink starts attaching a provider to the caller's own account.
	//
	// AAL2. identity.md §7 rule 3 requires step-up for linking, and the reason is
	// the whole of §7: a link attached to an account the linking party has not
	// proven control of is a TROJAN IDENTIFIER that survives every later recovery,
	// because a password reset changes a credential and leaves a link alone.
	//
	// No bootstrap exemption. Linking a provider is not a way to enrol a first
	// second factor, and admitting one here would make the weakest session able to
	// add a permanent way in.
	BeginFederatedLink(context.Context, *connect.Request[v1.BeginFederatedLinkRequest]) (*connect.Response[v1.BeginFederatedLinkResponse], error)
	// FinishFederatedLink completes the link.
	//
	// It consults NO auto-link rules, deliberately: the caller has already proven
	// the account, and that proof is exactly what §7 rule 1's conditions
	// substitute for when it is absent. Applying them again would refuse somebody
	// permission to link their own provider to their own account.
	FinishFederatedLink(context.Context, *connect.Request[v1.FinishFederatedLinkRequest]) (*connect.Response[v1.FinishFederatedLinkResponse], error)
	// UnlinkFederatedIdentity removes a provider from the caller's account.
	//
	// Refused when it would leave the account with no way to sign in — identity.md
	// §7's de-linking rule names the case exactly: removing the last federated
	// link from a PASSWORDLESS account, whose holder would then have nothing at
	// all. Somebody who signed up with Google has no password by design, so an
	// endpoint that allowed it would be account loss dressed as a settings toggle.
	UnlinkFederatedIdentity(context.Context, *connect.Request[v1.UnlinkFederatedIdentityRequest]) (*connect.Response[v1.UnlinkFederatedIdentityResponse], error)
	// CreateServiceAccount opens a non-human principal in the caller's
	// organization.
	//
	// It mints NO credential. A service account that has just been created can
	// authenticate nothing at all; CreateApiKey is a separate decision that
	// separately notifies, and keeping them apart is what lets an incident
	// timeline distinguish "somebody created a principal" from "somebody gave a
	// principal a way in".
	CreateServiceAccount(context.Context, *connect.Request[v1.CreateServiceAccountRequest]) (*connect.Response[v1.CreateServiceAccountResponse], error)
	// ListServiceAccounts shows the organization's non-human principals.
	//
	// READ, so it is permitted in every organization state including Suspended
	// and Closed — a tenant that has stopped paying may still see what
	// authenticates against it.
	ListServiceAccounts(context.Context, *connect.Request[v1.ListServiceAccountsRequest]) (*connect.Response[v1.ListServiceAccountsResponse], error)
	// CreateApiKey mints a machine credential and returns it exactly once.
	//
	// The token is in the response and nowhere else: only its digest is stored,
	// so this is the only moment it exists anywhere this system can reach. A
	// caller that loses it rotates the key.
	//
	// The key is bound to the caller's organization IMMUTABLY. Without that
	// binding a key silently inherits every organization its owner belongs to,
	// and a token leaked from one customer's CI reaches another customer's data —
	// a cross-tenant breach originating in a feature nobody thought was
	// tenant-scoped (identity.md §10, review D2).
	CreateApiKey(context.Context, *connect.Request[v1.CreateApiKeyRequest]) (*connect.Response[v1.CreateApiKeyResponse], error)
	// RotateApiKey replaces a key's secret without replacing the key.
	//
	// The key id, the scopes, the organization binding and every grant that names
	// the owner are unchanged, so no consumer has to be reconfigured beyond the
	// secret itself — which is what makes rotation something anybody actually
	// performs.
	//
	// The superseded secret keeps working for a bounded, RECORDED window, so
	// there is no instant in which neither secret resolves and no path by which
	// the old one lives forever. `immediate` collapses the window to nothing,
	// which is the leak response.
	RotateApiKey(context.Context, *connect.Request[v1.RotateApiKeyRequest]) (*connect.Response[v1.RotateApiKeyResponse], error)
	// RevokeApiKey ends a key and destroys every secret it ever had.
	//
	// IMMEDIATE, and not eventually: the stored digests are deleted in the same
	// request, and the event is what makes the projection and the audit trail
	// agree. Neither half is enough alone — the event alone leaves the credential
	// usable until the projector catches up, and the delete alone leaves nothing
	// in the log saying why the key stopped working, so a rebuild would show it
	// as live while it resolved nothing.
	//
	// Idempotent: revoking an already-revoked key appends nothing, reports
	// `changed = false`, and STILL sweeps secrets — a second call is what
	// somebody makes when they are not sure the first took.
	RevokeApiKey(context.Context, *connect.Request[v1.RevokeApiKeyRequest]) (*connect.Response[v1.RevokeApiKeyResponse], error)
	// ListApiKeys shows the organization's machine credentials, revoked ones
	// included.
	//
	// No token and no digest comes back, and no field of the response could hold
	// one. Revoked keys are listed on purpose: the evidence that a revocation
	// took effect is what somebody checks immediately after performing one.
	ListApiKeys(context.Context, *connect.Request[v1.ListApiKeysRequest]) (*connect.Response[v1.ListApiKeysResponse], error)
}

// NewIdentityServiceClient constructs a client for the chronos.identity.v1.IdentityService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewIdentityServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) IdentityServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	identityServiceMethods := v1.File_chronos_identity_v1_identity_proto.Services().ByName("IdentityService").Methods()
	return &identityServiceClient{
		register: connect.NewClient[v1.RegisterRequest, v1.RegisterResponse](
			httpClient,
			baseURL+IdentityServiceRegisterProcedure,
			connect.WithSchema(identityServiceMethods.ByName("Register")),
			connect.WithClientOptions(opts...),
		),
		verifyEmail: connect.NewClient[v1.VerifyEmailRequest, v1.VerifyEmailResponse](
			httpClient,
			baseURL+IdentityServiceVerifyEmailProcedure,
			connect.WithSchema(identityServiceMethods.ByName("VerifyEmail")),
			connect.WithClientOptions(opts...),
		),
		resendEmailVerification: connect.NewClient[v1.ResendEmailVerificationRequest, v1.ResendEmailVerificationResponse](
			httpClient,
			baseURL+IdentityServiceResendEmailVerificationProcedure,
			connect.WithSchema(identityServiceMethods.ByName("ResendEmailVerification")),
			connect.WithClientOptions(opts...),
		),
		checkUsernameAvailability: connect.NewClient[v1.CheckUsernameAvailabilityRequest, v1.CheckUsernameAvailabilityResponse](
			httpClient,
			baseURL+IdentityServiceCheckUsernameAvailabilityProcedure,
			connect.WithSchema(identityServiceMethods.ByName("CheckUsernameAvailability")),
			connect.WithClientOptions(opts...),
		),
		requestPasswordReset: connect.NewClient[v1.RequestPasswordResetRequest, v1.RequestPasswordResetResponse](
			httpClient,
			baseURL+IdentityServiceRequestPasswordResetProcedure,
			connect.WithSchema(identityServiceMethods.ByName("RequestPasswordReset")),
			connect.WithClientOptions(opts...),
		),
		resetPassword: connect.NewClient[v1.ResetPasswordRequest, v1.ResetPasswordResponse](
			httpClient,
			baseURL+IdentityServiceResetPasswordProcedure,
			connect.WithSchema(identityServiceMethods.ByName("ResetPassword")),
			connect.WithClientOptions(opts...),
		),
		authenticate: connect.NewClient[v1.AuthenticateRequest, v1.AuthenticateResponse](
			httpClient,
			baseURL+IdentityServiceAuthenticateProcedure,
			connect.WithSchema(identityServiceMethods.ByName("Authenticate")),
			connect.WithClientOptions(opts...),
		),
		createSession: connect.NewClient[v1.CreateSessionRequest, v1.CreateSessionResponse](
			httpClient,
			baseURL+IdentityServiceCreateSessionProcedure,
			connect.WithSchema(identityServiceMethods.ByName("CreateSession")),
			connect.WithClientOptions(opts...),
		),
		getUser: connect.NewClient[v1.GetUserRequest, v1.GetUserResponse](
			httpClient,
			baseURL+IdentityServiceGetUserProcedure,
			connect.WithSchema(identityServiceMethods.ByName("GetUser")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		listSessions: connect.NewClient[v1.ListSessionsRequest, v1.ListSessionsResponse](
			httpClient,
			baseURL+IdentityServiceListSessionsProcedure,
			connect.WithSchema(identityServiceMethods.ByName("ListSessions")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		listMethods: connect.NewClient[v1.ListMethodsRequest, v1.ListMethodsResponse](
			httpClient,
			baseURL+IdentityServiceListMethodsProcedure,
			connect.WithSchema(identityServiceMethods.ByName("ListMethods")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		listLoginHistory: connect.NewClient[v1.ListLoginHistoryRequest, v1.ListLoginHistoryResponse](
			httpClient,
			baseURL+IdentityServiceListLoginHistoryProcedure,
			connect.WithSchema(identityServiceMethods.ByName("ListLoginHistory")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		enrollTotp: connect.NewClient[v1.EnrollTotpRequest, v1.EnrollTotpResponse](
			httpClient,
			baseURL+IdentityServiceEnrollTotpProcedure,
			connect.WithSchema(identityServiceMethods.ByName("EnrollTotp")),
			connect.WithClientOptions(opts...),
		),
		confirmTotp: connect.NewClient[v1.ConfirmTotpRequest, v1.ConfirmTotpResponse](
			httpClient,
			baseURL+IdentityServiceConfirmTotpProcedure,
			connect.WithSchema(identityServiceMethods.ByName("ConfirmTotp")),
			connect.WithClientOptions(opts...),
		),
		generateRecoveryCodes: connect.NewClient[v1.GenerateRecoveryCodesRequest, v1.GenerateRecoveryCodesResponse](
			httpClient,
			baseURL+IdentityServiceGenerateRecoveryCodesProcedure,
			connect.WithSchema(identityServiceMethods.ByName("GenerateRecoveryCodes")),
			connect.WithClientOptions(opts...),
		),
		revokeSession: connect.NewClient[v1.RevokeSessionRequest, v1.RevokeSessionResponse](
			httpClient,
			baseURL+IdentityServiceRevokeSessionProcedure,
			connect.WithSchema(identityServiceMethods.ByName("RevokeSession")),
			connect.WithClientOptions(opts...),
		),
		revokeAllSessions: connect.NewClient[v1.RevokeAllSessionsRequest, v1.RevokeAllSessionsResponse](
			httpClient,
			baseURL+IdentityServiceRevokeAllSessionsProcedure,
			connect.WithSchema(identityServiceMethods.ByName("RevokeAllSessions")),
			connect.WithClientOptions(opts...),
		),
		deactivateAccount: connect.NewClient[v1.DeactivateAccountRequest, v1.DeactivateAccountResponse](
			httpClient,
			baseURL+IdentityServiceDeactivateAccountProcedure,
			connect.WithSchema(identityServiceMethods.ByName("DeactivateAccount")),
			connect.WithClientOptions(opts...),
		),
		requestAccountDeletion: connect.NewClient[v1.RequestAccountDeletionRequest, v1.RequestAccountDeletionResponse](
			httpClient,
			baseURL+IdentityServiceRequestAccountDeletionProcedure,
			connect.WithSchema(identityServiceMethods.ByName("RequestAccountDeletion")),
			connect.WithClientOptions(opts...),
		),
		cancelAccountDeletion: connect.NewClient[v1.CancelAccountDeletionRequest, v1.CancelAccountDeletionResponse](
			httpClient,
			baseURL+IdentityServiceCancelAccountDeletionProcedure,
			connect.WithSchema(identityServiceMethods.ByName("CancelAccountDeletion")),
			connect.WithClientOptions(opts...),
		),
		requestEmailChange: connect.NewClient[v1.RequestEmailChangeRequest, v1.RequestEmailChangeResponse](
			httpClient,
			baseURL+IdentityServiceRequestEmailChangeProcedure,
			connect.WithSchema(identityServiceMethods.ByName("RequestEmailChange")),
			connect.WithClientOptions(opts...),
		),
		cancelEmailChange: connect.NewClient[v1.CancelEmailChangeRequest, v1.CancelEmailChangeResponse](
			httpClient,
			baseURL+IdentityServiceCancelEmailChangeProcedure,
			connect.WithSchema(identityServiceMethods.ByName("CancelEmailChange")),
			connect.WithClientOptions(opts...),
		),
		confirmEmailChange: connect.NewClient[v1.ConfirmEmailChangeRequest, v1.ConfirmEmailChangeResponse](
			httpClient,
			baseURL+IdentityServiceConfirmEmailChangeProcedure,
			connect.WithSchema(identityServiceMethods.ByName("ConfirmEmailChange")),
			connect.WithClientOptions(opts...),
		),
		revertEmailChange: connect.NewClient[v1.RevertEmailChangeRequest, v1.RevertEmailChangeResponse](
			httpClient,
			baseURL+IdentityServiceRevertEmailChangeProcedure,
			connect.WithSchema(identityServiceMethods.ByName("RevertEmailChange")),
			connect.WithClientOptions(opts...),
		),
		beginPasskeyRegistration: connect.NewClient[v1.BeginPasskeyRegistrationRequest, v1.BeginPasskeyRegistrationResponse](
			httpClient,
			baseURL+IdentityServiceBeginPasskeyRegistrationProcedure,
			connect.WithSchema(identityServiceMethods.ByName("BeginPasskeyRegistration")),
			connect.WithClientOptions(opts...),
		),
		finishPasskeyRegistration: connect.NewClient[v1.FinishPasskeyRegistrationRequest, v1.FinishPasskeyRegistrationResponse](
			httpClient,
			baseURL+IdentityServiceFinishPasskeyRegistrationProcedure,
			connect.WithSchema(identityServiceMethods.ByName("FinishPasskeyRegistration")),
			connect.WithClientOptions(opts...),
		),
		beginPasskeyLogin: connect.NewClient[v1.BeginPasskeyLoginRequest, v1.BeginPasskeyLoginResponse](
			httpClient,
			baseURL+IdentityServiceBeginPasskeyLoginProcedure,
			connect.WithSchema(identityServiceMethods.ByName("BeginPasskeyLogin")),
			connect.WithClientOptions(opts...),
		),
		finishPasskeyLogin: connect.NewClient[v1.FinishPasskeyLoginRequest, v1.FinishPasskeyLoginResponse](
			httpClient,
			baseURL+IdentityServiceFinishPasskeyLoginProcedure,
			connect.WithSchema(identityServiceMethods.ByName("FinishPasskeyLogin")),
			connect.WithClientOptions(opts...),
		),
		listPasskeys: connect.NewClient[v1.ListPasskeysRequest, v1.ListPasskeysResponse](
			httpClient,
			baseURL+IdentityServiceListPasskeysProcedure,
			connect.WithSchema(identityServiceMethods.ByName("ListPasskeys")),
			connect.WithClientOptions(opts...),
		),
		removePasskey: connect.NewClient[v1.RemovePasskeyRequest, v1.RemovePasskeyResponse](
			httpClient,
			baseURL+IdentityServiceRemovePasskeyProcedure,
			connect.WithSchema(identityServiceMethods.ByName("RemovePasskey")),
			connect.WithClientOptions(opts...),
		),
		listFederatedProviders: connect.NewClient[v1.ListFederatedProvidersRequest, v1.ListFederatedProvidersResponse](
			httpClient,
			baseURL+IdentityServiceListFederatedProvidersProcedure,
			connect.WithSchema(identityServiceMethods.ByName("ListFederatedProviders")),
			connect.WithClientOptions(opts...),
		),
		beginFederatedSignIn: connect.NewClient[v1.BeginFederatedSignInRequest, v1.BeginFederatedSignInResponse](
			httpClient,
			baseURL+IdentityServiceBeginFederatedSignInProcedure,
			connect.WithSchema(identityServiceMethods.ByName("BeginFederatedSignIn")),
			connect.WithClientOptions(opts...),
		),
		finishFederatedSignIn: connect.NewClient[v1.FinishFederatedSignInRequest, v1.FinishFederatedSignInResponse](
			httpClient,
			baseURL+IdentityServiceFinishFederatedSignInProcedure,
			connect.WithSchema(identityServiceMethods.ByName("FinishFederatedSignIn")),
			connect.WithClientOptions(opts...),
		),
		beginFederatedLink: connect.NewClient[v1.BeginFederatedLinkRequest, v1.BeginFederatedLinkResponse](
			httpClient,
			baseURL+IdentityServiceBeginFederatedLinkProcedure,
			connect.WithSchema(identityServiceMethods.ByName("BeginFederatedLink")),
			connect.WithClientOptions(opts...),
		),
		finishFederatedLink: connect.NewClient[v1.FinishFederatedLinkRequest, v1.FinishFederatedLinkResponse](
			httpClient,
			baseURL+IdentityServiceFinishFederatedLinkProcedure,
			connect.WithSchema(identityServiceMethods.ByName("FinishFederatedLink")),
			connect.WithClientOptions(opts...),
		),
		unlinkFederatedIdentity: connect.NewClient[v1.UnlinkFederatedIdentityRequest, v1.UnlinkFederatedIdentityResponse](
			httpClient,
			baseURL+IdentityServiceUnlinkFederatedIdentityProcedure,
			connect.WithSchema(identityServiceMethods.ByName("UnlinkFederatedIdentity")),
			connect.WithClientOptions(opts...),
		),
		createServiceAccount: connect.NewClient[v1.CreateServiceAccountRequest, v1.CreateServiceAccountResponse](
			httpClient,
			baseURL+IdentityServiceCreateServiceAccountProcedure,
			connect.WithSchema(identityServiceMethods.ByName("CreateServiceAccount")),
			connect.WithClientOptions(opts...),
		),
		listServiceAccounts: connect.NewClient[v1.ListServiceAccountsRequest, v1.ListServiceAccountsResponse](
			httpClient,
			baseURL+IdentityServiceListServiceAccountsProcedure,
			connect.WithSchema(identityServiceMethods.ByName("ListServiceAccounts")),
			connect.WithClientOptions(opts...),
		),
		createApiKey: connect.NewClient[v1.CreateApiKeyRequest, v1.CreateApiKeyResponse](
			httpClient,
			baseURL+IdentityServiceCreateApiKeyProcedure,
			connect.WithSchema(identityServiceMethods.ByName("CreateApiKey")),
			connect.WithClientOptions(opts...),
		),
		rotateApiKey: connect.NewClient[v1.RotateApiKeyRequest, v1.RotateApiKeyResponse](
			httpClient,
			baseURL+IdentityServiceRotateApiKeyProcedure,
			connect.WithSchema(identityServiceMethods.ByName("RotateApiKey")),
			connect.WithClientOptions(opts...),
		),
		revokeApiKey: connect.NewClient[v1.RevokeApiKeyRequest, v1.RevokeApiKeyResponse](
			httpClient,
			baseURL+IdentityServiceRevokeApiKeyProcedure,
			connect.WithSchema(identityServiceMethods.ByName("RevokeApiKey")),
			connect.WithClientOptions(opts...),
		),
		listApiKeys: connect.NewClient[v1.ListApiKeysRequest, v1.ListApiKeysResponse](
			httpClient,
			baseURL+IdentityServiceListApiKeysProcedure,
			connect.WithSchema(identityServiceMethods.ByName("ListApiKeys")),
			connect.WithClientOptions(opts...),
		),
	}
}

// identityServiceClient implements IdentityServiceClient.
type identityServiceClient struct {
	register                  *connect.Client[v1.RegisterRequest, v1.RegisterResponse]
	verifyEmail               *connect.Client[v1.VerifyEmailRequest, v1.VerifyEmailResponse]
	resendEmailVerification   *connect.Client[v1.ResendEmailVerificationRequest, v1.ResendEmailVerificationResponse]
	checkUsernameAvailability *connect.Client[v1.CheckUsernameAvailabilityRequest, v1.CheckUsernameAvailabilityResponse]
	requestPasswordReset      *connect.Client[v1.RequestPasswordResetRequest, v1.RequestPasswordResetResponse]
	resetPassword             *connect.Client[v1.ResetPasswordRequest, v1.ResetPasswordResponse]
	authenticate              *connect.Client[v1.AuthenticateRequest, v1.AuthenticateResponse]
	createSession             *connect.Client[v1.CreateSessionRequest, v1.CreateSessionResponse]
	getUser                   *connect.Client[v1.GetUserRequest, v1.GetUserResponse]
	listSessions              *connect.Client[v1.ListSessionsRequest, v1.ListSessionsResponse]
	listMethods               *connect.Client[v1.ListMethodsRequest, v1.ListMethodsResponse]
	listLoginHistory          *connect.Client[v1.ListLoginHistoryRequest, v1.ListLoginHistoryResponse]
	enrollTotp                *connect.Client[v1.EnrollTotpRequest, v1.EnrollTotpResponse]
	confirmTotp               *connect.Client[v1.ConfirmTotpRequest, v1.ConfirmTotpResponse]
	generateRecoveryCodes     *connect.Client[v1.GenerateRecoveryCodesRequest, v1.GenerateRecoveryCodesResponse]
	revokeSession             *connect.Client[v1.RevokeSessionRequest, v1.RevokeSessionResponse]
	revokeAllSessions         *connect.Client[v1.RevokeAllSessionsRequest, v1.RevokeAllSessionsResponse]
	deactivateAccount         *connect.Client[v1.DeactivateAccountRequest, v1.DeactivateAccountResponse]
	requestAccountDeletion    *connect.Client[v1.RequestAccountDeletionRequest, v1.RequestAccountDeletionResponse]
	cancelAccountDeletion     *connect.Client[v1.CancelAccountDeletionRequest, v1.CancelAccountDeletionResponse]
	requestEmailChange        *connect.Client[v1.RequestEmailChangeRequest, v1.RequestEmailChangeResponse]
	cancelEmailChange         *connect.Client[v1.CancelEmailChangeRequest, v1.CancelEmailChangeResponse]
	confirmEmailChange        *connect.Client[v1.ConfirmEmailChangeRequest, v1.ConfirmEmailChangeResponse]
	revertEmailChange         *connect.Client[v1.RevertEmailChangeRequest, v1.RevertEmailChangeResponse]
	beginPasskeyRegistration  *connect.Client[v1.BeginPasskeyRegistrationRequest, v1.BeginPasskeyRegistrationResponse]
	finishPasskeyRegistration *connect.Client[v1.FinishPasskeyRegistrationRequest, v1.FinishPasskeyRegistrationResponse]
	beginPasskeyLogin         *connect.Client[v1.BeginPasskeyLoginRequest, v1.BeginPasskeyLoginResponse]
	finishPasskeyLogin        *connect.Client[v1.FinishPasskeyLoginRequest, v1.FinishPasskeyLoginResponse]
	listPasskeys              *connect.Client[v1.ListPasskeysRequest, v1.ListPasskeysResponse]
	removePasskey             *connect.Client[v1.RemovePasskeyRequest, v1.RemovePasskeyResponse]
	listFederatedProviders    *connect.Client[v1.ListFederatedProvidersRequest, v1.ListFederatedProvidersResponse]
	beginFederatedSignIn      *connect.Client[v1.BeginFederatedSignInRequest, v1.BeginFederatedSignInResponse]
	finishFederatedSignIn     *connect.Client[v1.FinishFederatedSignInRequest, v1.FinishFederatedSignInResponse]
	beginFederatedLink        *connect.Client[v1.BeginFederatedLinkRequest, v1.BeginFederatedLinkResponse]
	finishFederatedLink       *connect.Client[v1.FinishFederatedLinkRequest, v1.FinishFederatedLinkResponse]
	unlinkFederatedIdentity   *connect.Client[v1.UnlinkFederatedIdentityRequest, v1.UnlinkFederatedIdentityResponse]
	createServiceAccount      *connect.Client[v1.CreateServiceAccountRequest, v1.CreateServiceAccountResponse]
	listServiceAccounts       *connect.Client[v1.ListServiceAccountsRequest, v1.ListServiceAccountsResponse]
	createApiKey              *connect.Client[v1.CreateApiKeyRequest, v1.CreateApiKeyResponse]
	rotateApiKey              *connect.Client[v1.RotateApiKeyRequest, v1.RotateApiKeyResponse]
	revokeApiKey              *connect.Client[v1.RevokeApiKeyRequest, v1.RevokeApiKeyResponse]
	listApiKeys               *connect.Client[v1.ListApiKeysRequest, v1.ListApiKeysResponse]
}

// Register calls chronos.identity.v1.IdentityService.Register.
func (c *identityServiceClient) Register(ctx context.Context, req *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error) {
	return c.register.CallUnary(ctx, req)
}

// VerifyEmail calls chronos.identity.v1.IdentityService.VerifyEmail.
func (c *identityServiceClient) VerifyEmail(ctx context.Context, req *connect.Request[v1.VerifyEmailRequest]) (*connect.Response[v1.VerifyEmailResponse], error) {
	return c.verifyEmail.CallUnary(ctx, req)
}

// ResendEmailVerification calls chronos.identity.v1.IdentityService.ResendEmailVerification.
func (c *identityServiceClient) ResendEmailVerification(ctx context.Context, req *connect.Request[v1.ResendEmailVerificationRequest]) (*connect.Response[v1.ResendEmailVerificationResponse], error) {
	return c.resendEmailVerification.CallUnary(ctx, req)
}

// CheckUsernameAvailability calls chronos.identity.v1.IdentityService.CheckUsernameAvailability.
func (c *identityServiceClient) CheckUsernameAvailability(ctx context.Context, req *connect.Request[v1.CheckUsernameAvailabilityRequest]) (*connect.Response[v1.CheckUsernameAvailabilityResponse], error) {
	return c.checkUsernameAvailability.CallUnary(ctx, req)
}

// RequestPasswordReset calls chronos.identity.v1.IdentityService.RequestPasswordReset.
func (c *identityServiceClient) RequestPasswordReset(ctx context.Context, req *connect.Request[v1.RequestPasswordResetRequest]) (*connect.Response[v1.RequestPasswordResetResponse], error) {
	return c.requestPasswordReset.CallUnary(ctx, req)
}

// ResetPassword calls chronos.identity.v1.IdentityService.ResetPassword.
func (c *identityServiceClient) ResetPassword(ctx context.Context, req *connect.Request[v1.ResetPasswordRequest]) (*connect.Response[v1.ResetPasswordResponse], error) {
	return c.resetPassword.CallUnary(ctx, req)
}

// Authenticate calls chronos.identity.v1.IdentityService.Authenticate.
func (c *identityServiceClient) Authenticate(ctx context.Context, req *connect.Request[v1.AuthenticateRequest]) (*connect.Response[v1.AuthenticateResponse], error) {
	return c.authenticate.CallUnary(ctx, req)
}

// CreateSession calls chronos.identity.v1.IdentityService.CreateSession.
func (c *identityServiceClient) CreateSession(ctx context.Context, req *connect.Request[v1.CreateSessionRequest]) (*connect.Response[v1.CreateSessionResponse], error) {
	return c.createSession.CallUnary(ctx, req)
}

// GetUser calls chronos.identity.v1.IdentityService.GetUser.
func (c *identityServiceClient) GetUser(ctx context.Context, req *connect.Request[v1.GetUserRequest]) (*connect.Response[v1.GetUserResponse], error) {
	return c.getUser.CallUnary(ctx, req)
}

// ListSessions calls chronos.identity.v1.IdentityService.ListSessions.
func (c *identityServiceClient) ListSessions(ctx context.Context, req *connect.Request[v1.ListSessionsRequest]) (*connect.Response[v1.ListSessionsResponse], error) {
	return c.listSessions.CallUnary(ctx, req)
}

// ListMethods calls chronos.identity.v1.IdentityService.ListMethods.
func (c *identityServiceClient) ListMethods(ctx context.Context, req *connect.Request[v1.ListMethodsRequest]) (*connect.Response[v1.ListMethodsResponse], error) {
	return c.listMethods.CallUnary(ctx, req)
}

// ListLoginHistory calls chronos.identity.v1.IdentityService.ListLoginHistory.
func (c *identityServiceClient) ListLoginHistory(ctx context.Context, req *connect.Request[v1.ListLoginHistoryRequest]) (*connect.Response[v1.ListLoginHistoryResponse], error) {
	return c.listLoginHistory.CallUnary(ctx, req)
}

// EnrollTotp calls chronos.identity.v1.IdentityService.EnrollTotp.
func (c *identityServiceClient) EnrollTotp(ctx context.Context, req *connect.Request[v1.EnrollTotpRequest]) (*connect.Response[v1.EnrollTotpResponse], error) {
	return c.enrollTotp.CallUnary(ctx, req)
}

// ConfirmTotp calls chronos.identity.v1.IdentityService.ConfirmTotp.
func (c *identityServiceClient) ConfirmTotp(ctx context.Context, req *connect.Request[v1.ConfirmTotpRequest]) (*connect.Response[v1.ConfirmTotpResponse], error) {
	return c.confirmTotp.CallUnary(ctx, req)
}

// GenerateRecoveryCodes calls chronos.identity.v1.IdentityService.GenerateRecoveryCodes.
func (c *identityServiceClient) GenerateRecoveryCodes(ctx context.Context, req *connect.Request[v1.GenerateRecoveryCodesRequest]) (*connect.Response[v1.GenerateRecoveryCodesResponse], error) {
	return c.generateRecoveryCodes.CallUnary(ctx, req)
}

// RevokeSession calls chronos.identity.v1.IdentityService.RevokeSession.
func (c *identityServiceClient) RevokeSession(ctx context.Context, req *connect.Request[v1.RevokeSessionRequest]) (*connect.Response[v1.RevokeSessionResponse], error) {
	return c.revokeSession.CallUnary(ctx, req)
}

// RevokeAllSessions calls chronos.identity.v1.IdentityService.RevokeAllSessions.
func (c *identityServiceClient) RevokeAllSessions(ctx context.Context, req *connect.Request[v1.RevokeAllSessionsRequest]) (*connect.Response[v1.RevokeAllSessionsResponse], error) {
	return c.revokeAllSessions.CallUnary(ctx, req)
}

// DeactivateAccount calls chronos.identity.v1.IdentityService.DeactivateAccount.
func (c *identityServiceClient) DeactivateAccount(ctx context.Context, req *connect.Request[v1.DeactivateAccountRequest]) (*connect.Response[v1.DeactivateAccountResponse], error) {
	return c.deactivateAccount.CallUnary(ctx, req)
}

// RequestAccountDeletion calls chronos.identity.v1.IdentityService.RequestAccountDeletion.
func (c *identityServiceClient) RequestAccountDeletion(ctx context.Context, req *connect.Request[v1.RequestAccountDeletionRequest]) (*connect.Response[v1.RequestAccountDeletionResponse], error) {
	return c.requestAccountDeletion.CallUnary(ctx, req)
}

// CancelAccountDeletion calls chronos.identity.v1.IdentityService.CancelAccountDeletion.
func (c *identityServiceClient) CancelAccountDeletion(ctx context.Context, req *connect.Request[v1.CancelAccountDeletionRequest]) (*connect.Response[v1.CancelAccountDeletionResponse], error) {
	return c.cancelAccountDeletion.CallUnary(ctx, req)
}

// RequestEmailChange calls chronos.identity.v1.IdentityService.RequestEmailChange.
func (c *identityServiceClient) RequestEmailChange(ctx context.Context, req *connect.Request[v1.RequestEmailChangeRequest]) (*connect.Response[v1.RequestEmailChangeResponse], error) {
	return c.requestEmailChange.CallUnary(ctx, req)
}

// CancelEmailChange calls chronos.identity.v1.IdentityService.CancelEmailChange.
func (c *identityServiceClient) CancelEmailChange(ctx context.Context, req *connect.Request[v1.CancelEmailChangeRequest]) (*connect.Response[v1.CancelEmailChangeResponse], error) {
	return c.cancelEmailChange.CallUnary(ctx, req)
}

// ConfirmEmailChange calls chronos.identity.v1.IdentityService.ConfirmEmailChange.
func (c *identityServiceClient) ConfirmEmailChange(ctx context.Context, req *connect.Request[v1.ConfirmEmailChangeRequest]) (*connect.Response[v1.ConfirmEmailChangeResponse], error) {
	return c.confirmEmailChange.CallUnary(ctx, req)
}

// RevertEmailChange calls chronos.identity.v1.IdentityService.RevertEmailChange.
func (c *identityServiceClient) RevertEmailChange(ctx context.Context, req *connect.Request[v1.RevertEmailChangeRequest]) (*connect.Response[v1.RevertEmailChangeResponse], error) {
	return c.revertEmailChange.CallUnary(ctx, req)
}

// BeginPasskeyRegistration calls chronos.identity.v1.IdentityService.BeginPasskeyRegistration.
func (c *identityServiceClient) BeginPasskeyRegistration(ctx context.Context, req *connect.Request[v1.BeginPasskeyRegistrationRequest]) (*connect.Response[v1.BeginPasskeyRegistrationResponse], error) {
	return c.beginPasskeyRegistration.CallUnary(ctx, req)
}

// FinishPasskeyRegistration calls chronos.identity.v1.IdentityService.FinishPasskeyRegistration.
func (c *identityServiceClient) FinishPasskeyRegistration(ctx context.Context, req *connect.Request[v1.FinishPasskeyRegistrationRequest]) (*connect.Response[v1.FinishPasskeyRegistrationResponse], error) {
	return c.finishPasskeyRegistration.CallUnary(ctx, req)
}

// BeginPasskeyLogin calls chronos.identity.v1.IdentityService.BeginPasskeyLogin.
func (c *identityServiceClient) BeginPasskeyLogin(ctx context.Context, req *connect.Request[v1.BeginPasskeyLoginRequest]) (*connect.Response[v1.BeginPasskeyLoginResponse], error) {
	return c.beginPasskeyLogin.CallUnary(ctx, req)
}

// FinishPasskeyLogin calls chronos.identity.v1.IdentityService.FinishPasskeyLogin.
func (c *identityServiceClient) FinishPasskeyLogin(ctx context.Context, req *connect.Request[v1.FinishPasskeyLoginRequest]) (*connect.Response[v1.FinishPasskeyLoginResponse], error) {
	return c.finishPasskeyLogin.CallUnary(ctx, req)
}

// ListPasskeys calls chronos.identity.v1.IdentityService.ListPasskeys.
func (c *identityServiceClient) ListPasskeys(ctx context.Context, req *connect.Request[v1.ListPasskeysRequest]) (*connect.Response[v1.ListPasskeysResponse], error) {
	return c.listPasskeys.CallUnary(ctx, req)
}

// RemovePasskey calls chronos.identity.v1.IdentityService.RemovePasskey.
func (c *identityServiceClient) RemovePasskey(ctx context.Context, req *connect.Request[v1.RemovePasskeyRequest]) (*connect.Response[v1.RemovePasskeyResponse], error) {
	return c.removePasskey.CallUnary(ctx, req)
}

// ListFederatedProviders calls chronos.identity.v1.IdentityService.ListFederatedProviders.
func (c *identityServiceClient) ListFederatedProviders(ctx context.Context, req *connect.Request[v1.ListFederatedProvidersRequest]) (*connect.Response[v1.ListFederatedProvidersResponse], error) {
	return c.listFederatedProviders.CallUnary(ctx, req)
}

// BeginFederatedSignIn calls chronos.identity.v1.IdentityService.BeginFederatedSignIn.
func (c *identityServiceClient) BeginFederatedSignIn(ctx context.Context, req *connect.Request[v1.BeginFederatedSignInRequest]) (*connect.Response[v1.BeginFederatedSignInResponse], error) {
	return c.beginFederatedSignIn.CallUnary(ctx, req)
}

// FinishFederatedSignIn calls chronos.identity.v1.IdentityService.FinishFederatedSignIn.
func (c *identityServiceClient) FinishFederatedSignIn(ctx context.Context, req *connect.Request[v1.FinishFederatedSignInRequest]) (*connect.Response[v1.FinishFederatedSignInResponse], error) {
	return c.finishFederatedSignIn.CallUnary(ctx, req)
}

// BeginFederatedLink calls chronos.identity.v1.IdentityService.BeginFederatedLink.
func (c *identityServiceClient) BeginFederatedLink(ctx context.Context, req *connect.Request[v1.BeginFederatedLinkRequest]) (*connect.Response[v1.BeginFederatedLinkResponse], error) {
	return c.beginFederatedLink.CallUnary(ctx, req)
}

// FinishFederatedLink calls chronos.identity.v1.IdentityService.FinishFederatedLink.
func (c *identityServiceClient) FinishFederatedLink(ctx context.Context, req *connect.Request[v1.FinishFederatedLinkRequest]) (*connect.Response[v1.FinishFederatedLinkResponse], error) {
	return c.finishFederatedLink.CallUnary(ctx, req)
}

// UnlinkFederatedIdentity calls chronos.identity.v1.IdentityService.UnlinkFederatedIdentity.
func (c *identityServiceClient) UnlinkFederatedIdentity(ctx context.Context, req *connect.Request[v1.UnlinkFederatedIdentityRequest]) (*connect.Response[v1.UnlinkFederatedIdentityResponse], error) {
	return c.unlinkFederatedIdentity.CallUnary(ctx, req)
}

// CreateServiceAccount calls chronos.identity.v1.IdentityService.CreateServiceAccount.
func (c *identityServiceClient) CreateServiceAccount(ctx context.Context, req *connect.Request[v1.CreateServiceAccountRequest]) (*connect.Response[v1.CreateServiceAccountResponse], error) {
	return c.createServiceAccount.CallUnary(ctx, req)
}

// ListServiceAccounts calls chronos.identity.v1.IdentityService.ListServiceAccounts.
func (c *identityServiceClient) ListServiceAccounts(ctx context.Context, req *connect.Request[v1.ListServiceAccountsRequest]) (*connect.Response[v1.ListServiceAccountsResponse], error) {
	return c.listServiceAccounts.CallUnary(ctx, req)
}

// CreateApiKey calls chronos.identity.v1.IdentityService.CreateApiKey.
func (c *identityServiceClient) CreateApiKey(ctx context.Context, req *connect.Request[v1.CreateApiKeyRequest]) (*connect.Response[v1.CreateApiKeyResponse], error) {
	return c.createApiKey.CallUnary(ctx, req)
}

// RotateApiKey calls chronos.identity.v1.IdentityService.RotateApiKey.
func (c *identityServiceClient) RotateApiKey(ctx context.Context, req *connect.Request[v1.RotateApiKeyRequest]) (*connect.Response[v1.RotateApiKeyResponse], error) {
	return c.rotateApiKey.CallUnary(ctx, req)
}

// RevokeApiKey calls chronos.identity.v1.IdentityService.RevokeApiKey.
func (c *identityServiceClient) RevokeApiKey(ctx context.Context, req *connect.Request[v1.RevokeApiKeyRequest]) (*connect.Response[v1.RevokeApiKeyResponse], error) {
	return c.revokeApiKey.CallUnary(ctx, req)
}

// ListApiKeys calls chronos.identity.v1.IdentityService.ListApiKeys.
func (c *identityServiceClient) ListApiKeys(ctx context.Context, req *connect.Request[v1.ListApiKeysRequest]) (*connect.Response[v1.ListApiKeysResponse], error) {
	return c.listApiKeys.CallUnary(ctx, req)
}

// IdentityServiceHandler is an implementation of the chronos.identity.v1.IdentityService service.
type IdentityServiceHandler interface {
	// Register claims an address and creates the account that claims it.
	//
	// Public: the caller has no session and, by definition, no account. It is a
	// WRITE rather than a GROW — a person is not a seat, and seats are consumed
	// when an organization adds a member, not when a human being signs up.
	//
	// It sets NO PASSWORD and creates no credential of any kind. The account is
	// unusable by everybody until the link mailed to the address is followed and
	// VerifyEmail supplies one.
	//
	// The response is identical whether or not the address was free. See
	// RegisterResponse.
	Register(context.Context, *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error)
	// VerifyEmail proves control of an address, sets the account's first
	// password, and makes the claim on the address permanent.
	//
	// Public: it is reached from a link in a mailbox, by a browser that has never
	// authenticated. The token IS the authentication, and it is single-use.
	//
	// It is the ONLY call that can give a brand-new account a password, and that
	// is the pre-hijacking defence: the credential is created by the party that
	// has just proven the mailbox, in the same request as the proof.
	VerifyEmail(context.Context, *connect.Request[v1.VerifyEmailRequest]) (*connect.Response[v1.VerifyEmailResponse], error)
	// ResendEmailVerification issues a new verification link for an address whose
	// account has not proven it yet.
	//
	// Public, and the choice is forced rather than preferred. A Pending account
	// can hold no session, so requiring one would make this call unavailable to
	// every account that needs it — the endpoint would exist and serve nobody.
	//
	// The cost of being public is that it is an unauthenticated mail trigger
	// pointed at a third party's mailbox, so it is rate limited on TWO axes before
	// anything is looked up: per address, which bounds mail-bombing one victim, and
	// per caller, which bounds an enumeration sweep. Both counters are consumed
	// whether or not an account exists, so the ceiling itself is not an oracle.
	//
	// A WRITE: the successful path appends EmailVerificationRequested to the
	// account's stream, and the verification reactor does the rest.
	ResendEmailVerification(context.Context, *connect.Request[v1.ResendEmailVerificationRequest]) (*connect.Response[v1.ResendEmailVerificationResponse], error)
	// CheckUsernameAvailability reports whether a public handle can be claimed.
	//
	// Public: it is reached from the signup form, by a browser that has never
	// authenticated and by definition has no account.
	//
	// # It IS an enumeration oracle, and that is the intended behaviour
	//
	// A caller can learn that @alice is taken. That is not a leak, because a handle
	// is PUBLISHED — publication is its entire purpose (ADR-051) — and the same
	// fact is readable from any page that renders a mention, from a profile URL,
	// and from the person themselves. Every other public RPC in this service is
	// shaped to make its two outcomes indistinguishable; this one is shaped to make
	// them distinguishable, and the difference is not an oversight in the schema.
	//
	// Do NOT "fix" this into an undifferentiated response. The resulting endpoint
	// would answer no question at all, the signup form could not tell a person
	// their handle was taken until after they had spent a verification link they
	// cannot get back, and the information would remain freely available everywhere
	// else in the product.
	//
	// The ONE thing it must not distinguish is a tombstoned handle from a taken
	// one; see CheckUsernameAvailabilityResponse for why that single merge is a
	// privacy control rather than the same tidiness.
	//
	// # The answer is advisory
	//
	// It reads the handle's reservation stream at an instant. Uniqueness is
	// enforced at the moment of the WRITE by an atomic append with a precondition
	// (ADR-044, ADR-051), so a handle reported free may be taken by the time
	// VerifyEmail claims it — any check-then-claim is racy by construction. The
	// point of this call is that a person finds out at the FORM rather than after
	// clicking a link.
	//
	// A READ: it appends nothing, mints nothing and sends no mail. It is rate
	// limited per caller regardless — "an attacker may learn whether @alice exists"
	// is not the same permission as "an attacker may read a stream from the event
	// store as fast as they can open sockets".
	CheckUsernameAvailability(context.Context, *connect.Request[v1.CheckUsernameAvailabilityRequest]) (*connect.Response[v1.CheckUsernameAvailabilityResponse], error)
	// RequestPasswordReset sends a reset link to the address a registered account
	// holds.
	//
	// Public, and the choice is forced rather than preferred: the population that
	// needs this call is exactly the population that cannot sign in.
	//
	// The cost of being public is that it is an unauthenticated mail trigger
	// pointed at a third party's mailbox, so it is rate limited on TWO axes before
	// anything is looked up — per address, which bounds mail-bombing one victim,
	// and per caller, which bounds an enumeration sweep. Both counters are
	// consumed whether or not an account exists, so the ceiling itself is not an
	// oracle. The per-address counter is the SAME one verification mail spends
	// (NOTIFICATIONS.md §4: "an hourly ceiling per address across all classes"), so
	// an attacker cannot double a victim's mail by alternating between the two
	// endpoints.
	//
	// A WRITE: the successful path appends PasswordResetRequested to the account's
	// stream, and the reset-mail issuer does the rest.
	RequestPasswordReset(context.Context, *connect.Request[v1.RequestPasswordResetRequest]) (*connect.Response[v1.RequestPasswordResetResponse], error)
	// ResetPassword redeems a reset link and replaces the account's password.
	//
	// Public: it is reached from a link in a mailbox, by a browser that has never
	// authenticated. The token IS the authentication, and it is single-use.
	//
	// It grants NOTHING beyond the credential change. No session is created, no
	// bearer token is returned, the second factor is untouched, and a Pending
	// account is not activated — so a reset cannot be used to bypass the second
	// factor (ASVS 5.0 V6.4.3). The caller's next act is an ordinary
	// CreateSession.
	//
	// In the same request it voids every session for the account including any
	// held by whoever is resetting, every outstanding token of every purpose, and
	// any pending identifier change (identity.md §4.4, §4.5).
	ResetPassword(context.Context, *connect.Request[v1.ResetPasswordRequest]) (*connect.Response[v1.ResetPasswordResponse], error)
	// Authenticate verifies the factors presented and reports what remains
	// (ADR-050).
	//
	// Public: this is where a session comes from, so requiring one is circular.
	//
	// A WRITE, and not a read despite minting nothing: every attempt appends to
	// the authentication journal, and classing it as a read would let it through
	// gates whose whole purpose is to stop writes. There is no `min_aal` for the
	// same reason there is no authz — the caller has no session to have a level.
	Authenticate(context.Context, *connect.Request[v1.AuthenticateRequest]) (*connect.Response[v1.AuthenticateResponse], error)
	// CreateSession completes a login and mints the bearer token.
	//
	// Public, for the same reason as Authenticate: it is the call that creates the
	// session everything else requires.
	CreateSession(context.Context, *connect.Request[v1.CreateSessionRequest]) (*connect.Response[v1.CreateSessionResponse], error)
	// GetUser returns the caller's own account: what it is, where it is in its
	// lifecycle, and whether its address has been proven.
	//
	// AAL1 — no `min_aal` declared, so the default applies. This discloses nothing
	// an attacker holding a session does not already have, and demanding step-up
	// to render the account screen would make step-up routine, which is how people
	// learn to approve it without reading it.
	GetUser(context.Context, *connect.Request[v1.GetUserRequest]) (*connect.Response[v1.GetUserResponse], error)
	// ListSessions returns the caller's device list, newest first, one page at a
	// time.
	//
	// AAL1. It is a read of a list the holder needs in order to NOTICE a
	// compromise, and putting step-up in front of noticing is backwards — the
	// revocation that follows is what carries the AAL2 requirement.
	ListSessions(context.Context, *connect.Request[v1.ListSessionsRequest]) (*connect.Response[v1.ListSessionsResponse], error)
	// ListMethods returns the authentication methods on the caller's account:
	// which exist, what kind each is, whether it can be used now, and when it last
	// was.
	//
	// AAL1, on the same argument as ListSessions: this is the screen on which
	// somebody discovers an authenticator they did not enrol.
	ListMethods(context.Context, *connect.Request[v1.ListMethodsRequest]) (*connect.Response[v1.ListMethodsResponse], error)
	// ListLoginHistory returns recent authentication attempts against the caller's
	// account, successes and failures alike, newest first.
	//
	// AAL1, and deliberately so even though this is the most sensitive read here.
	// It is the account holder's own security log, and it is only useful if they
	// actually look at it.
	ListLoginHistory(context.Context, *connect.Request[v1.ListLoginHistoryRequest]) (*connect.Response[v1.ListLoginHistoryResponse], error)
	// EnrollTotp provisions a new authenticator secret and returns it once.
	//
	// AAL2. Enrolling a second factor is how an attacker holding one factor makes
	// their access durable, so it must not be reachable from a session that only
	// ever presented one. It replaces any unproven enrolment on the account, so
	// the abandoned secret stops existing at the moment the new one is written
	// rather than lingering as a second confirmable factor.
	//
	// AAL1 while the account has never held a proven second factor, and only
	// then. Read the two levels together and they say: presenting the existing
	// factor is what buys the right to add another, and an account with no
	// existing factor is exempt from a demand nothing could satisfy. Without the
	// exemption an account registered through this API could never activate — the
	// factor it must have to reach AAL2 is the factor this call exists to give it
	// (identity.md §2).
	//
	// The exemption is not a weaker version of the same permission. It applies to
	// a strictly poorer target: an account with no second factor, whose password
	// is therefore already the only thing standing between an attacker and it. An
	// attacker who enrols there has gained nothing they did not already have. On
	// an account that HAS a factor the floor is AAL2, so the same attacker must
	// present that factor — and it does not re-open when a factor is removed,
	// because the window closes on first proof and never re-opens.
	EnrollTotp(context.Context, *connect.Request[v1.EnrollTotpRequest]) (*connect.Response[v1.EnrollTotpResponse], error)
	// ConfirmTotp proves possession of a provisioned secret, and may be what
	// activates the account.
	//
	// AAL2, matching EnrollTotp: a confirmation is the half of an enrolment that
	// makes a factor real, and letting it run at a lower level than the enrolment
	// itself would leave the pair only as strong as its weaker call.
	//
	// The bootstrap floor matches EnrollTotp for the same reason. The two calls
	// are one operation split in half, and exempting only the first would leave
	// an account able to provision a secret it could never prove — the deadlock
	// moved one call to the right rather than removed.
	//
	// This is the call that CLOSES the window: the account it activates has a
	// proven second factor from that moment, so the next call to either method
	// faces AAL2. The exemption is therefore self-limiting, and it is limited by
	// the very thing it was granted to produce.
	ConfirmTotp(context.Context, *connect.Request[v1.ConfirmTotpRequest]) (*connect.Response[v1.ConfirmTotpResponse], error)
	// GenerateRecoveryCodes replaces the whole recovery-code set and returns the
	// new plaintext codes once.
	//
	// AAL2. A recovery code is a standing bypass of every other factor, so minting
	// a fresh set from a single-factor session would hand an attacker a permanent
	// credential that survives the password change the victim makes in response.
	// Generating codes never activates an account — only a real second factor
	// does.
	//
	// NO bootstrap exemption, deliberately, and this is the method where the
	// decision is load-bearing. The exemption exists to break a deadlock, and
	// there is no deadlock here: recovery codes cannot activate an account, so
	// nothing about the path from registered to Active runs through this call. An
	// account reaches its first AAL2 session by confirming a TOTP secret, and only
	// then asks for codes — by which time the strict floor is satisfiable. Granting
	// the exemption anyway would mean a single-factor session could mint the one
	// credential that outlives every remedy the account holder has, on an account
	// that has no second factor to lose. That is the exact hole the floor is
	// shaped to keep shut, and it would be opened for no gain.
	GenerateRecoveryCodes(context.Context, *connect.Request[v1.GenerateRecoveryCodesRequest]) (*connect.Response[v1.GenerateRecoveryCodesResponse], error)
	// RevokeSession ends one of the caller's sessions.
	//
	// AAL2. Revocation is the control a person reaches for when they believe they
	// are compromised, and it must not be usable BY the compromise: a
	// single-factor session that could end sessions could sign the legitimate
	// holder out of the device they are using to investigate.
	RevokeSession(context.Context, *connect.Request[v1.RevokeSessionRequest]) (*connect.Response[v1.RevokeSessionResponse], error)
	// RevokeAllSessions ends every live session on the caller's account, in one
	// atomic append.
	//
	// AAL2, on the same argument as RevokeSession and more so: this is the
	// account-wide version, so the denial of service an attacker could inflict
	// with it is total.
	RevokeAllSessions(context.Context, *connect.Request[v1.RevokeAllSessionsRequest]) (*connect.Response[v1.RevokeAllSessionsResponse], error)
	// DeactivateAccount switches the caller's own account off, and ends every
	// session on it in the same atomic append.
	//
	// # Reversible, and how
	//
	// Deactivation is the holder's own switch and the holder undoes it by SIGNING
	// IN again with every factor the account has — the successful authentication
	// records the reactivation in the same append as itself, so the account is on
	// again before the new session exists. There is deliberately no
	// `ReactivateAccount` RPC: it would need a session, a session needs an
	// authentication, and an authentication refuses a deactivated account, so its
	// only precondition would be one its own subject can never satisfy. See
	// `identity.md` §1.
	//
	// Suspension is the administrative counterpart and is NOT here. It is not
	// reversible by the holder, every method on this service is reached by the
	// holder acting on their own account, and there is no operator surface in this
	// repository yet — so an RPC for it could only be a self-suspension, which
	// makes the account unreachable by every route this module has.
	//
	// # AAL2
	//
	// On RevokeAllSessions' argument: the sign-out is total, so the denial of
	// service an attacker holding a stolen password could inflict with it is total.
	// No `bootstrap_min_aal`, deliberately — an account still enrolling its first
	// factor has nothing to switch off, and the exemption exists for enrolment
	// alone.
	DeactivateAccount(context.Context, *connect.Request[v1.DeactivateAccountRequest]) (*connect.Response[v1.DeactivateAccountResponse], error)
	// RequestAccountDeletion asks for the caller's own account to be erased, and
	// starts the grace period.
	//
	// # The account keeps working until the deadline
	//
	// A request is not a deletion. `compliance` runs the grace period as a durable
	// workflow and erases at the end of it; until then everything works normally,
	// and `CancelAccountDeletion` withdraws the request.
	//
	// This is stated on the RPC rather than only in a design document because a
	// client that reads "deletion requested" as "account gone" will build a screen
	// that lies to the person using it — and the screen that matters most here is
	// the one offering the way back.
	//
	// # AAL2, plus a typed confirmation
	//
	// identity.md §2 lists account deletion among the operations that require
	// step-up. The step-up proves WHO is asking; the `confirmation` field is what
	// makes it hard to ask by ACCIDENT — a distinct property, and the one
	// cancellation cannot cover, because cancelling requires somebody to notice
	// and the person who never meant to ask is the least likely to.
	//
	// # It does not sign anybody out
	//
	// Every other destructive call here voids sessions. This one deliberately does
	// not: the grace period exists so the person can change their mind, and signing
	// them out of an account that still works teaches them the request took effect
	// immediately, which it did not.
	RequestAccountDeletion(context.Context, *connect.Request[v1.RequestAccountDeletionRequest]) (*connect.Response[v1.RequestAccountDeletionResponse], error)
	// CancelAccountDeletion withdraws the caller's own outstanding erasure request.
	//
	// # This is what makes the grace period real
	//
	// A waiting period nobody can use is a delay, not a safeguard. The window
	// exists so that somebody who clicked in anger, or whose session was taken,
	// can stop what was started — and the erasure workflow re-reads on every wake
	// precisely so that this call stops it.
	//
	// Cancelling when nothing is outstanding SUCCEEDS and records nothing. The
	// cancel link in the "deletion scheduled" mail gets clicked twice, or after an
	// operator already withdrew the request; neither person did anything wrong and
	// neither should be told they did.
	//
	// # AAL2, and no typed confirmation
	//
	// Step-up for the same reason requesting needs it: this decides whether an
	// account survives, and an attacker holding a low-assurance session must not be
	// able to keep alive an account its owner is trying to close.
	//
	// No confirmation field, and the asymmetry is deliberate. A typed confirmation
	// guards something that cannot be undone; this IS the undo, and putting a
	// hurdle in front of somebody who has just realised their mistake protects
	// nothing.
	//
	// # Why it notifies
	//
	// The catalogue mails a Security-class message on cancellation, which reads as
	// reassurance and is not only that. Somebody who deliberately asked for erasure
	// and whose request was withdrawn by SOMEBODY ELSE needs to know: that is
	// exactly what an attacker with a stolen session does when the holder tries to
	// close the account under them.
	//
	// Returns `NOT_FOUND` for an account already erased — there is nothing to come
	// back to, and saying so specifically would confirm that a particular person
	// once held it.
	CancelAccountDeletion(context.Context, *connect.Request[v1.CancelAccountDeletionRequest]) (*connect.Response[v1.CancelAccountDeletionResponse], error)
	// RequestEmailChange starts a move to a different email address.
	//
	// Nothing about the account moves here, and that is the design rather than a
	// limitation: identity.md §12 requires the NEW address to be proven first,
	// because otherwise an attacker holding a session takes ownership by naming an
	// address they cannot read. This claims the address and mails a link to it.
	//
	// Two messages go out. The link goes to the new address. A SECURITY warning
	// goes to the address the account uses today — with no link and not even the
	// new address in it, since that mailbox is exactly the one an attacker may
	// already be reading. That warning is what makes the flow survivable, and it
	// is why this RPC is worth having rather than an administrator's edit.
	//
	// AAL2, because identity.md §4.3 lists an email change among the operations
	// that require step-up. There is no `bootstrap_min_aal`: an account with no
	// second factor has no business moving its recovery address, and the bootstrap
	// exemption exists to let somebody ENROL a factor, not to widen what a
	// single-factor session may do.
	RequestEmailChange(context.Context, *connect.Request[v1.RequestEmailChangeRequest]) (*connect.Response[v1.RequestEmailChangeResponse], error)
	// CancelEmailChange calls off a pending change.
	//
	// The action the security warning tells somebody to take. It releases the
	// claim on the address and VOIDS the link already sitting in that mailbox, so
	// a change the account holder refused cannot complete afterwards.
	//
	// Idempotent: cancelling when nothing is pending is success, because the
	// caller asked for a state that already holds.
	CancelEmailChange(context.Context, *connect.Request[v1.CancelEmailChangeRequest]) (*connect.Response[v1.CancelEmailChangeResponse], error)
	// ConfirmEmailChange redeems the link mailed to the new address.
	//
	// Public: it is reached from a mailbox by a browser that has never
	// authenticated — and after this call, could not be, because completing the
	// change voids every session on the account (identity.md §4.4). The token IS
	// the authentication and it is single-use.
	//
	// It grants nothing beyond the address move. No session is created and no
	// bearer is returned; the caller's next act is an ordinary CreateSession with
	// the new address.
	//
	// The old address is NOT released. It is held for a revert window and a link
	// to undo the change is mailed to it, which is the remedy for the case where
	// the party who proved the new address was not the account's owner.
	ConfirmEmailChange(context.Context, *connect.Request[v1.ConfirmEmailChangeRequest]) (*connect.Response[v1.ConfirmEmailChangeResponse], error)
	// RevertEmailChange undoes a completed change.
	//
	// Public, and reached from a link mailed to the address the account moved AWAY
	// from — so whoever redeems it has proven control of the address the account
	// had BEFORE the change. That is the whole remedy identity.md §12 asks for: an
	// attacker holding a session can move the address, and cannot stop the real
	// owner being told and undoing it.
	//
	// It voids every session again, for a sharper reason than the change did: the
	// party this is being undone against may still be holding the session they did
	// it from.
	//
	// The window is finite. After it the old address is available to anybody, so
	// this refuses rather than taking it back from whoever claimed it since.
	RevertEmailChange(context.Context, *connect.Request[v1.RevertEmailChangeRequest]) (*connect.Response[v1.RevertEmailChangeResponse], error)
	// BeginPasskeyRegistration starts enrolling an authenticator.
	//
	// Returns the JSON for `navigator.credentials.create` and an opaque ceremony
	// id. Nothing is enrolled until FinishPasskeyRegistration verifies what the
	// authenticator signed.
	//
	// AAL2: adding a credential that can sign in on its own is a credential
	// change, and identity.md §4.3 requires step-up for those.
	BeginPasskeyRegistration(context.Context, *connect.Request[v1.BeginPasskeyRegistrationRequest]) (*connect.Response[v1.BeginPasskeyRegistrationResponse], error)
	// FinishPasskeyRegistration verifies the authenticator's answer and enrols it.
	//
	// The ceremony is SINGLE-USE: replaying one is refused, because one signature
	// must not become two credentials.
	//
	// On an account's FIRST passkey this returns recovery codes, once.
	// identity.md §5 calls lockout "the real design problem" — somebody whose only
	// method is a passkey on a lost device must still get back in — so the codes
	// are issued here rather than offered afterwards, because an afterthought is a
	// path most people never take.
	FinishPasskeyRegistration(context.Context, *connect.Request[v1.FinishPasskeyRegistrationRequest]) (*connect.Response[v1.FinishPasskeyRegistrationResponse], error)
	// BeginPasskeyLogin starts a usernameless sign-in.
	//
	// PUBLIC, and it carries NO identifier. That absence is the feature: the
	// authenticator names the account, so this endpoint cannot be asked whether an
	// address exists. A field for one would reintroduce the enumeration oracle
	// every other public surface in this module removes.
	//
	// It looks up nothing and answers identically whether or not any account
	// exists.
	BeginPasskeyLogin(context.Context, *connect.Request[v1.BeginPasskeyLoginRequest]) (*connect.Response[v1.BeginPasskeyLoginResponse], error)
	// FinishPasskeyLogin verifies an assertion and creates a session.
	//
	// Public: the assertion IS the authentication. A user-verified passkey reaches
	// AAL2 on its own (identity.md §2) — the authenticator is the possession
	// factor and the PIN or biometric that unlocked it is the second — so this can
	// mint a full session in one gesture. Without user verification it reaches
	// AAL1 and anything sensitive will ask for step-up.
	//
	// A signature counter that went BACKWARDS does NOT refuse the sign-in. Most
	// synced passkeys report a counter of zero forever, and identity.md §5 is
	// explicit that treating a regression as fatal "locks out legitimate users".
	// The session is capped at AAL1 instead and the response says so.
	FinishPasskeyLogin(context.Context, *connect.Request[v1.FinishPasskeyLoginRequest]) (*connect.Response[v1.FinishPasskeyLoginResponse], error)
	// ListPasskeys shows the caller their own enrolled authenticators.
	//
	// The security screen. It returns the label the person chose, when each was
	// added and last used, and whether one has ever produced a counter regression
	// — which is what lets somebody notice a credential they do not recognise.
	ListPasskeys(context.Context, *connect.Request[v1.ListPasskeysRequest]) (*connect.Response[v1.ListPasskeysResponse], error)
	// RemovePasskey deletes one of the caller's authenticators.
	//
	// Refused when it would leave the account with no way to sign in, which is the
	// same rule that stops the last second factor being removed. Somebody deleting
	// their only passkey from a device they no longer have is not helped by an
	// endpoint that lets them.
	//
	// AAL2 with no bootstrap exemption: removing a credential is not a way to
	// enrol one.
	RemovePasskey(context.Context, *connect.Request[v1.RemovePasskeyRequest]) (*connect.Response[v1.RemovePasskeyResponse], error)
	// ListFederatedProviders names the identity providers this deployment
	// supports.
	//
	// PUBLIC and deliberately uninteresting: it says what the DEPLOYMENT
	// supports, never what any account uses. A client needs it to render buttons,
	// and it discloses nothing about anybody.
	ListFederatedProviders(context.Context, *connect.Request[v1.ListFederatedProvidersRequest]) (*connect.Response[v1.ListFederatedProvidersResponse], error)
	// BeginFederatedSignIn starts signing in with an identity provider.
	//
	// Public: the population reaching it has not authenticated, and by definition
	// may have no account at all. It carries no identifier, so it cannot be asked
	// whether an address exists.
	//
	// A WRITE, because it records a ceremony: PKCE, state and nonce are bound to
	// this request and stored server-side, which is what makes the callback
	// verifiable rather than merely plausible.
	BeginFederatedSignIn(context.Context, *connect.Request[v1.BeginFederatedSignInRequest]) (*connect.Response[v1.BeginFederatedSignInResponse], error)
	// FinishFederatedSignIn redeems the provider's callback.
	//
	// # It does NOT always produce a session, and that is the design
	//
	// identity.md §7 forbids auto-linking a federated identity to an existing
	// account on an email match alone — the attacker registers at a provider using
	// the victim's address, the provider does not verify it, and naive matching
	// hands them the account. So a genuine sign-in whose conditions are not met
	// answers `link_refused` with no session, and the person authenticates with an
	// existing method and links explicitly from settings.
	//
	// `account_exists` rides with it so a client can say "you already have an
	// account, sign in and link this" rather than the dead end §7.5 names, where a
	// second account is created silently and one person owns two.
	//
	// The session it does create is AAL1: a provider may have required its own
	// second factor and this system cannot know that it did.
	FinishFederatedSignIn(context.Context, *connect.Request[v1.FinishFederatedSignInRequest]) (*connect.Response[v1.FinishFederatedSignInResponse], error)
	// BeginFederatedLink starts attaching a provider to the caller's own account.
	//
	// AAL2. identity.md §7 rule 3 requires step-up for linking, and the reason is
	// the whole of §7: a link attached to an account the linking party has not
	// proven control of is a TROJAN IDENTIFIER that survives every later recovery,
	// because a password reset changes a credential and leaves a link alone.
	//
	// No bootstrap exemption. Linking a provider is not a way to enrol a first
	// second factor, and admitting one here would make the weakest session able to
	// add a permanent way in.
	BeginFederatedLink(context.Context, *connect.Request[v1.BeginFederatedLinkRequest]) (*connect.Response[v1.BeginFederatedLinkResponse], error)
	// FinishFederatedLink completes the link.
	//
	// It consults NO auto-link rules, deliberately: the caller has already proven
	// the account, and that proof is exactly what §7 rule 1's conditions
	// substitute for when it is absent. Applying them again would refuse somebody
	// permission to link their own provider to their own account.
	FinishFederatedLink(context.Context, *connect.Request[v1.FinishFederatedLinkRequest]) (*connect.Response[v1.FinishFederatedLinkResponse], error)
	// UnlinkFederatedIdentity removes a provider from the caller's account.
	//
	// Refused when it would leave the account with no way to sign in — identity.md
	// §7's de-linking rule names the case exactly: removing the last federated
	// link from a PASSWORDLESS account, whose holder would then have nothing at
	// all. Somebody who signed up with Google has no password by design, so an
	// endpoint that allowed it would be account loss dressed as a settings toggle.
	UnlinkFederatedIdentity(context.Context, *connect.Request[v1.UnlinkFederatedIdentityRequest]) (*connect.Response[v1.UnlinkFederatedIdentityResponse], error)
	// CreateServiceAccount opens a non-human principal in the caller's
	// organization.
	//
	// It mints NO credential. A service account that has just been created can
	// authenticate nothing at all; CreateApiKey is a separate decision that
	// separately notifies, and keeping them apart is what lets an incident
	// timeline distinguish "somebody created a principal" from "somebody gave a
	// principal a way in".
	CreateServiceAccount(context.Context, *connect.Request[v1.CreateServiceAccountRequest]) (*connect.Response[v1.CreateServiceAccountResponse], error)
	// ListServiceAccounts shows the organization's non-human principals.
	//
	// READ, so it is permitted in every organization state including Suspended
	// and Closed — a tenant that has stopped paying may still see what
	// authenticates against it.
	ListServiceAccounts(context.Context, *connect.Request[v1.ListServiceAccountsRequest]) (*connect.Response[v1.ListServiceAccountsResponse], error)
	// CreateApiKey mints a machine credential and returns it exactly once.
	//
	// The token is in the response and nowhere else: only its digest is stored,
	// so this is the only moment it exists anywhere this system can reach. A
	// caller that loses it rotates the key.
	//
	// The key is bound to the caller's organization IMMUTABLY. Without that
	// binding a key silently inherits every organization its owner belongs to,
	// and a token leaked from one customer's CI reaches another customer's data —
	// a cross-tenant breach originating in a feature nobody thought was
	// tenant-scoped (identity.md §10, review D2).
	CreateApiKey(context.Context, *connect.Request[v1.CreateApiKeyRequest]) (*connect.Response[v1.CreateApiKeyResponse], error)
	// RotateApiKey replaces a key's secret without replacing the key.
	//
	// The key id, the scopes, the organization binding and every grant that names
	// the owner are unchanged, so no consumer has to be reconfigured beyond the
	// secret itself — which is what makes rotation something anybody actually
	// performs.
	//
	// The superseded secret keeps working for a bounded, RECORDED window, so
	// there is no instant in which neither secret resolves and no path by which
	// the old one lives forever. `immediate` collapses the window to nothing,
	// which is the leak response.
	RotateApiKey(context.Context, *connect.Request[v1.RotateApiKeyRequest]) (*connect.Response[v1.RotateApiKeyResponse], error)
	// RevokeApiKey ends a key and destroys every secret it ever had.
	//
	// IMMEDIATE, and not eventually: the stored digests are deleted in the same
	// request, and the event is what makes the projection and the audit trail
	// agree. Neither half is enough alone — the event alone leaves the credential
	// usable until the projector catches up, and the delete alone leaves nothing
	// in the log saying why the key stopped working, so a rebuild would show it
	// as live while it resolved nothing.
	//
	// Idempotent: revoking an already-revoked key appends nothing, reports
	// `changed = false`, and STILL sweeps secrets — a second call is what
	// somebody makes when they are not sure the first took.
	RevokeApiKey(context.Context, *connect.Request[v1.RevokeApiKeyRequest]) (*connect.Response[v1.RevokeApiKeyResponse], error)
	// ListApiKeys shows the organization's machine credentials, revoked ones
	// included.
	//
	// No token and no digest comes back, and no field of the response could hold
	// one. Revoked keys are listed on purpose: the evidence that a revocation
	// took effect is what somebody checks immediately after performing one.
	ListApiKeys(context.Context, *connect.Request[v1.ListApiKeysRequest]) (*connect.Response[v1.ListApiKeysResponse], error)
}

// NewIdentityServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewIdentityServiceHandler(svc IdentityServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	identityServiceMethods := v1.File_chronos_identity_v1_identity_proto.Services().ByName("IdentityService").Methods()
	identityServiceRegisterHandler := connect.NewUnaryHandler(
		IdentityServiceRegisterProcedure,
		svc.Register,
		connect.WithSchema(identityServiceMethods.ByName("Register")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceVerifyEmailHandler := connect.NewUnaryHandler(
		IdentityServiceVerifyEmailProcedure,
		svc.VerifyEmail,
		connect.WithSchema(identityServiceMethods.ByName("VerifyEmail")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceResendEmailVerificationHandler := connect.NewUnaryHandler(
		IdentityServiceResendEmailVerificationProcedure,
		svc.ResendEmailVerification,
		connect.WithSchema(identityServiceMethods.ByName("ResendEmailVerification")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceCheckUsernameAvailabilityHandler := connect.NewUnaryHandler(
		IdentityServiceCheckUsernameAvailabilityProcedure,
		svc.CheckUsernameAvailability,
		connect.WithSchema(identityServiceMethods.ByName("CheckUsernameAvailability")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceRequestPasswordResetHandler := connect.NewUnaryHandler(
		IdentityServiceRequestPasswordResetProcedure,
		svc.RequestPasswordReset,
		connect.WithSchema(identityServiceMethods.ByName("RequestPasswordReset")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceResetPasswordHandler := connect.NewUnaryHandler(
		IdentityServiceResetPasswordProcedure,
		svc.ResetPassword,
		connect.WithSchema(identityServiceMethods.ByName("ResetPassword")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceAuthenticateHandler := connect.NewUnaryHandler(
		IdentityServiceAuthenticateProcedure,
		svc.Authenticate,
		connect.WithSchema(identityServiceMethods.ByName("Authenticate")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceCreateSessionHandler := connect.NewUnaryHandler(
		IdentityServiceCreateSessionProcedure,
		svc.CreateSession,
		connect.WithSchema(identityServiceMethods.ByName("CreateSession")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceGetUserHandler := connect.NewUnaryHandler(
		IdentityServiceGetUserProcedure,
		svc.GetUser,
		connect.WithSchema(identityServiceMethods.ByName("GetUser")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceListSessionsHandler := connect.NewUnaryHandler(
		IdentityServiceListSessionsProcedure,
		svc.ListSessions,
		connect.WithSchema(identityServiceMethods.ByName("ListSessions")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceListMethodsHandler := connect.NewUnaryHandler(
		IdentityServiceListMethodsProcedure,
		svc.ListMethods,
		connect.WithSchema(identityServiceMethods.ByName("ListMethods")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceListLoginHistoryHandler := connect.NewUnaryHandler(
		IdentityServiceListLoginHistoryProcedure,
		svc.ListLoginHistory,
		connect.WithSchema(identityServiceMethods.ByName("ListLoginHistory")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceEnrollTotpHandler := connect.NewUnaryHandler(
		IdentityServiceEnrollTotpProcedure,
		svc.EnrollTotp,
		connect.WithSchema(identityServiceMethods.ByName("EnrollTotp")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceConfirmTotpHandler := connect.NewUnaryHandler(
		IdentityServiceConfirmTotpProcedure,
		svc.ConfirmTotp,
		connect.WithSchema(identityServiceMethods.ByName("ConfirmTotp")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceGenerateRecoveryCodesHandler := connect.NewUnaryHandler(
		IdentityServiceGenerateRecoveryCodesProcedure,
		svc.GenerateRecoveryCodes,
		connect.WithSchema(identityServiceMethods.ByName("GenerateRecoveryCodes")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceRevokeSessionHandler := connect.NewUnaryHandler(
		IdentityServiceRevokeSessionProcedure,
		svc.RevokeSession,
		connect.WithSchema(identityServiceMethods.ByName("RevokeSession")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceRevokeAllSessionsHandler := connect.NewUnaryHandler(
		IdentityServiceRevokeAllSessionsProcedure,
		svc.RevokeAllSessions,
		connect.WithSchema(identityServiceMethods.ByName("RevokeAllSessions")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceDeactivateAccountHandler := connect.NewUnaryHandler(
		IdentityServiceDeactivateAccountProcedure,
		svc.DeactivateAccount,
		connect.WithSchema(identityServiceMethods.ByName("DeactivateAccount")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceRequestAccountDeletionHandler := connect.NewUnaryHandler(
		IdentityServiceRequestAccountDeletionProcedure,
		svc.RequestAccountDeletion,
		connect.WithSchema(identityServiceMethods.ByName("RequestAccountDeletion")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceCancelAccountDeletionHandler := connect.NewUnaryHandler(
		IdentityServiceCancelAccountDeletionProcedure,
		svc.CancelAccountDeletion,
		connect.WithSchema(identityServiceMethods.ByName("CancelAccountDeletion")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceRequestEmailChangeHandler := connect.NewUnaryHandler(
		IdentityServiceRequestEmailChangeProcedure,
		svc.RequestEmailChange,
		connect.WithSchema(identityServiceMethods.ByName("RequestEmailChange")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceCancelEmailChangeHandler := connect.NewUnaryHandler(
		IdentityServiceCancelEmailChangeProcedure,
		svc.CancelEmailChange,
		connect.WithSchema(identityServiceMethods.ByName("CancelEmailChange")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceConfirmEmailChangeHandler := connect.NewUnaryHandler(
		IdentityServiceConfirmEmailChangeProcedure,
		svc.ConfirmEmailChange,
		connect.WithSchema(identityServiceMethods.ByName("ConfirmEmailChange")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceRevertEmailChangeHandler := connect.NewUnaryHandler(
		IdentityServiceRevertEmailChangeProcedure,
		svc.RevertEmailChange,
		connect.WithSchema(identityServiceMethods.ByName("RevertEmailChange")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceBeginPasskeyRegistrationHandler := connect.NewUnaryHandler(
		IdentityServiceBeginPasskeyRegistrationProcedure,
		svc.BeginPasskeyRegistration,
		connect.WithSchema(identityServiceMethods.ByName("BeginPasskeyRegistration")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceFinishPasskeyRegistrationHandler := connect.NewUnaryHandler(
		IdentityServiceFinishPasskeyRegistrationProcedure,
		svc.FinishPasskeyRegistration,
		connect.WithSchema(identityServiceMethods.ByName("FinishPasskeyRegistration")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceBeginPasskeyLoginHandler := connect.NewUnaryHandler(
		IdentityServiceBeginPasskeyLoginProcedure,
		svc.BeginPasskeyLogin,
		connect.WithSchema(identityServiceMethods.ByName("BeginPasskeyLogin")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceFinishPasskeyLoginHandler := connect.NewUnaryHandler(
		IdentityServiceFinishPasskeyLoginProcedure,
		svc.FinishPasskeyLogin,
		connect.WithSchema(identityServiceMethods.ByName("FinishPasskeyLogin")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceListPasskeysHandler := connect.NewUnaryHandler(
		IdentityServiceListPasskeysProcedure,
		svc.ListPasskeys,
		connect.WithSchema(identityServiceMethods.ByName("ListPasskeys")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceRemovePasskeyHandler := connect.NewUnaryHandler(
		IdentityServiceRemovePasskeyProcedure,
		svc.RemovePasskey,
		connect.WithSchema(identityServiceMethods.ByName("RemovePasskey")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceListFederatedProvidersHandler := connect.NewUnaryHandler(
		IdentityServiceListFederatedProvidersProcedure,
		svc.ListFederatedProviders,
		connect.WithSchema(identityServiceMethods.ByName("ListFederatedProviders")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceBeginFederatedSignInHandler := connect.NewUnaryHandler(
		IdentityServiceBeginFederatedSignInProcedure,
		svc.BeginFederatedSignIn,
		connect.WithSchema(identityServiceMethods.ByName("BeginFederatedSignIn")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceFinishFederatedSignInHandler := connect.NewUnaryHandler(
		IdentityServiceFinishFederatedSignInProcedure,
		svc.FinishFederatedSignIn,
		connect.WithSchema(identityServiceMethods.ByName("FinishFederatedSignIn")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceBeginFederatedLinkHandler := connect.NewUnaryHandler(
		IdentityServiceBeginFederatedLinkProcedure,
		svc.BeginFederatedLink,
		connect.WithSchema(identityServiceMethods.ByName("BeginFederatedLink")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceFinishFederatedLinkHandler := connect.NewUnaryHandler(
		IdentityServiceFinishFederatedLinkProcedure,
		svc.FinishFederatedLink,
		connect.WithSchema(identityServiceMethods.ByName("FinishFederatedLink")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceUnlinkFederatedIdentityHandler := connect.NewUnaryHandler(
		IdentityServiceUnlinkFederatedIdentityProcedure,
		svc.UnlinkFederatedIdentity,
		connect.WithSchema(identityServiceMethods.ByName("UnlinkFederatedIdentity")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceCreateServiceAccountHandler := connect.NewUnaryHandler(
		IdentityServiceCreateServiceAccountProcedure,
		svc.CreateServiceAccount,
		connect.WithSchema(identityServiceMethods.ByName("CreateServiceAccount")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceListServiceAccountsHandler := connect.NewUnaryHandler(
		IdentityServiceListServiceAccountsProcedure,
		svc.ListServiceAccounts,
		connect.WithSchema(identityServiceMethods.ByName("ListServiceAccounts")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceCreateApiKeyHandler := connect.NewUnaryHandler(
		IdentityServiceCreateApiKeyProcedure,
		svc.CreateApiKey,
		connect.WithSchema(identityServiceMethods.ByName("CreateApiKey")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceRotateApiKeyHandler := connect.NewUnaryHandler(
		IdentityServiceRotateApiKeyProcedure,
		svc.RotateApiKey,
		connect.WithSchema(identityServiceMethods.ByName("RotateApiKey")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceRevokeApiKeyHandler := connect.NewUnaryHandler(
		IdentityServiceRevokeApiKeyProcedure,
		svc.RevokeApiKey,
		connect.WithSchema(identityServiceMethods.ByName("RevokeApiKey")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceListApiKeysHandler := connect.NewUnaryHandler(
		IdentityServiceListApiKeysProcedure,
		svc.ListApiKeys,
		connect.WithSchema(identityServiceMethods.ByName("ListApiKeys")),
		connect.WithHandlerOptions(opts...),
	)
	return "/chronos.identity.v1.IdentityService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case IdentityServiceRegisterProcedure:
			identityServiceRegisterHandler.ServeHTTP(w, r)
		case IdentityServiceVerifyEmailProcedure:
			identityServiceVerifyEmailHandler.ServeHTTP(w, r)
		case IdentityServiceResendEmailVerificationProcedure:
			identityServiceResendEmailVerificationHandler.ServeHTTP(w, r)
		case IdentityServiceCheckUsernameAvailabilityProcedure:
			identityServiceCheckUsernameAvailabilityHandler.ServeHTTP(w, r)
		case IdentityServiceRequestPasswordResetProcedure:
			identityServiceRequestPasswordResetHandler.ServeHTTP(w, r)
		case IdentityServiceResetPasswordProcedure:
			identityServiceResetPasswordHandler.ServeHTTP(w, r)
		case IdentityServiceAuthenticateProcedure:
			identityServiceAuthenticateHandler.ServeHTTP(w, r)
		case IdentityServiceCreateSessionProcedure:
			identityServiceCreateSessionHandler.ServeHTTP(w, r)
		case IdentityServiceGetUserProcedure:
			identityServiceGetUserHandler.ServeHTTP(w, r)
		case IdentityServiceListSessionsProcedure:
			identityServiceListSessionsHandler.ServeHTTP(w, r)
		case IdentityServiceListMethodsProcedure:
			identityServiceListMethodsHandler.ServeHTTP(w, r)
		case IdentityServiceListLoginHistoryProcedure:
			identityServiceListLoginHistoryHandler.ServeHTTP(w, r)
		case IdentityServiceEnrollTotpProcedure:
			identityServiceEnrollTotpHandler.ServeHTTP(w, r)
		case IdentityServiceConfirmTotpProcedure:
			identityServiceConfirmTotpHandler.ServeHTTP(w, r)
		case IdentityServiceGenerateRecoveryCodesProcedure:
			identityServiceGenerateRecoveryCodesHandler.ServeHTTP(w, r)
		case IdentityServiceRevokeSessionProcedure:
			identityServiceRevokeSessionHandler.ServeHTTP(w, r)
		case IdentityServiceRevokeAllSessionsProcedure:
			identityServiceRevokeAllSessionsHandler.ServeHTTP(w, r)
		case IdentityServiceDeactivateAccountProcedure:
			identityServiceDeactivateAccountHandler.ServeHTTP(w, r)
		case IdentityServiceRequestAccountDeletionProcedure:
			identityServiceRequestAccountDeletionHandler.ServeHTTP(w, r)
		case IdentityServiceCancelAccountDeletionProcedure:
			identityServiceCancelAccountDeletionHandler.ServeHTTP(w, r)
		case IdentityServiceRequestEmailChangeProcedure:
			identityServiceRequestEmailChangeHandler.ServeHTTP(w, r)
		case IdentityServiceCancelEmailChangeProcedure:
			identityServiceCancelEmailChangeHandler.ServeHTTP(w, r)
		case IdentityServiceConfirmEmailChangeProcedure:
			identityServiceConfirmEmailChangeHandler.ServeHTTP(w, r)
		case IdentityServiceRevertEmailChangeProcedure:
			identityServiceRevertEmailChangeHandler.ServeHTTP(w, r)
		case IdentityServiceBeginPasskeyRegistrationProcedure:
			identityServiceBeginPasskeyRegistrationHandler.ServeHTTP(w, r)
		case IdentityServiceFinishPasskeyRegistrationProcedure:
			identityServiceFinishPasskeyRegistrationHandler.ServeHTTP(w, r)
		case IdentityServiceBeginPasskeyLoginProcedure:
			identityServiceBeginPasskeyLoginHandler.ServeHTTP(w, r)
		case IdentityServiceFinishPasskeyLoginProcedure:
			identityServiceFinishPasskeyLoginHandler.ServeHTTP(w, r)
		case IdentityServiceListPasskeysProcedure:
			identityServiceListPasskeysHandler.ServeHTTP(w, r)
		case IdentityServiceRemovePasskeyProcedure:
			identityServiceRemovePasskeyHandler.ServeHTTP(w, r)
		case IdentityServiceListFederatedProvidersProcedure:
			identityServiceListFederatedProvidersHandler.ServeHTTP(w, r)
		case IdentityServiceBeginFederatedSignInProcedure:
			identityServiceBeginFederatedSignInHandler.ServeHTTP(w, r)
		case IdentityServiceFinishFederatedSignInProcedure:
			identityServiceFinishFederatedSignInHandler.ServeHTTP(w, r)
		case IdentityServiceBeginFederatedLinkProcedure:
			identityServiceBeginFederatedLinkHandler.ServeHTTP(w, r)
		case IdentityServiceFinishFederatedLinkProcedure:
			identityServiceFinishFederatedLinkHandler.ServeHTTP(w, r)
		case IdentityServiceUnlinkFederatedIdentityProcedure:
			identityServiceUnlinkFederatedIdentityHandler.ServeHTTP(w, r)
		case IdentityServiceCreateServiceAccountProcedure:
			identityServiceCreateServiceAccountHandler.ServeHTTP(w, r)
		case IdentityServiceListServiceAccountsProcedure:
			identityServiceListServiceAccountsHandler.ServeHTTP(w, r)
		case IdentityServiceCreateApiKeyProcedure:
			identityServiceCreateApiKeyHandler.ServeHTTP(w, r)
		case IdentityServiceRotateApiKeyProcedure:
			identityServiceRotateApiKeyHandler.ServeHTTP(w, r)
		case IdentityServiceRevokeApiKeyProcedure:
			identityServiceRevokeApiKeyHandler.ServeHTTP(w, r)
		case IdentityServiceListApiKeysProcedure:
			identityServiceListApiKeysHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedIdentityServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedIdentityServiceHandler struct{}

func (UnimplementedIdentityServiceHandler) Register(context.Context, *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.Register is not implemented"))
}

func (UnimplementedIdentityServiceHandler) VerifyEmail(context.Context, *connect.Request[v1.VerifyEmailRequest]) (*connect.Response[v1.VerifyEmailResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.VerifyEmail is not implemented"))
}

func (UnimplementedIdentityServiceHandler) ResendEmailVerification(context.Context, *connect.Request[v1.ResendEmailVerificationRequest]) (*connect.Response[v1.ResendEmailVerificationResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.ResendEmailVerification is not implemented"))
}

func (UnimplementedIdentityServiceHandler) CheckUsernameAvailability(context.Context, *connect.Request[v1.CheckUsernameAvailabilityRequest]) (*connect.Response[v1.CheckUsernameAvailabilityResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.CheckUsernameAvailability is not implemented"))
}

func (UnimplementedIdentityServiceHandler) RequestPasswordReset(context.Context, *connect.Request[v1.RequestPasswordResetRequest]) (*connect.Response[v1.RequestPasswordResetResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.RequestPasswordReset is not implemented"))
}

func (UnimplementedIdentityServiceHandler) ResetPassword(context.Context, *connect.Request[v1.ResetPasswordRequest]) (*connect.Response[v1.ResetPasswordResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.ResetPassword is not implemented"))
}

func (UnimplementedIdentityServiceHandler) Authenticate(context.Context, *connect.Request[v1.AuthenticateRequest]) (*connect.Response[v1.AuthenticateResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.Authenticate is not implemented"))
}

func (UnimplementedIdentityServiceHandler) CreateSession(context.Context, *connect.Request[v1.CreateSessionRequest]) (*connect.Response[v1.CreateSessionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.CreateSession is not implemented"))
}

func (UnimplementedIdentityServiceHandler) GetUser(context.Context, *connect.Request[v1.GetUserRequest]) (*connect.Response[v1.GetUserResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.GetUser is not implemented"))
}

func (UnimplementedIdentityServiceHandler) ListSessions(context.Context, *connect.Request[v1.ListSessionsRequest]) (*connect.Response[v1.ListSessionsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.ListSessions is not implemented"))
}

func (UnimplementedIdentityServiceHandler) ListMethods(context.Context, *connect.Request[v1.ListMethodsRequest]) (*connect.Response[v1.ListMethodsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.ListMethods is not implemented"))
}

func (UnimplementedIdentityServiceHandler) ListLoginHistory(context.Context, *connect.Request[v1.ListLoginHistoryRequest]) (*connect.Response[v1.ListLoginHistoryResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.ListLoginHistory is not implemented"))
}

func (UnimplementedIdentityServiceHandler) EnrollTotp(context.Context, *connect.Request[v1.EnrollTotpRequest]) (*connect.Response[v1.EnrollTotpResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.EnrollTotp is not implemented"))
}

func (UnimplementedIdentityServiceHandler) ConfirmTotp(context.Context, *connect.Request[v1.ConfirmTotpRequest]) (*connect.Response[v1.ConfirmTotpResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.ConfirmTotp is not implemented"))
}

func (UnimplementedIdentityServiceHandler) GenerateRecoveryCodes(context.Context, *connect.Request[v1.GenerateRecoveryCodesRequest]) (*connect.Response[v1.GenerateRecoveryCodesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.GenerateRecoveryCodes is not implemented"))
}

func (UnimplementedIdentityServiceHandler) RevokeSession(context.Context, *connect.Request[v1.RevokeSessionRequest]) (*connect.Response[v1.RevokeSessionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.RevokeSession is not implemented"))
}

func (UnimplementedIdentityServiceHandler) RevokeAllSessions(context.Context, *connect.Request[v1.RevokeAllSessionsRequest]) (*connect.Response[v1.RevokeAllSessionsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.RevokeAllSessions is not implemented"))
}

func (UnimplementedIdentityServiceHandler) DeactivateAccount(context.Context, *connect.Request[v1.DeactivateAccountRequest]) (*connect.Response[v1.DeactivateAccountResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.DeactivateAccount is not implemented"))
}

func (UnimplementedIdentityServiceHandler) RequestAccountDeletion(context.Context, *connect.Request[v1.RequestAccountDeletionRequest]) (*connect.Response[v1.RequestAccountDeletionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.RequestAccountDeletion is not implemented"))
}

func (UnimplementedIdentityServiceHandler) CancelAccountDeletion(context.Context, *connect.Request[v1.CancelAccountDeletionRequest]) (*connect.Response[v1.CancelAccountDeletionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.CancelAccountDeletion is not implemented"))
}

func (UnimplementedIdentityServiceHandler) RequestEmailChange(context.Context, *connect.Request[v1.RequestEmailChangeRequest]) (*connect.Response[v1.RequestEmailChangeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.RequestEmailChange is not implemented"))
}

func (UnimplementedIdentityServiceHandler) CancelEmailChange(context.Context, *connect.Request[v1.CancelEmailChangeRequest]) (*connect.Response[v1.CancelEmailChangeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.CancelEmailChange is not implemented"))
}

func (UnimplementedIdentityServiceHandler) ConfirmEmailChange(context.Context, *connect.Request[v1.ConfirmEmailChangeRequest]) (*connect.Response[v1.ConfirmEmailChangeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.ConfirmEmailChange is not implemented"))
}

func (UnimplementedIdentityServiceHandler) RevertEmailChange(context.Context, *connect.Request[v1.RevertEmailChangeRequest]) (*connect.Response[v1.RevertEmailChangeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.RevertEmailChange is not implemented"))
}

func (UnimplementedIdentityServiceHandler) BeginPasskeyRegistration(context.Context, *connect.Request[v1.BeginPasskeyRegistrationRequest]) (*connect.Response[v1.BeginPasskeyRegistrationResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.BeginPasskeyRegistration is not implemented"))
}

func (UnimplementedIdentityServiceHandler) FinishPasskeyRegistration(context.Context, *connect.Request[v1.FinishPasskeyRegistrationRequest]) (*connect.Response[v1.FinishPasskeyRegistrationResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.FinishPasskeyRegistration is not implemented"))
}

func (UnimplementedIdentityServiceHandler) BeginPasskeyLogin(context.Context, *connect.Request[v1.BeginPasskeyLoginRequest]) (*connect.Response[v1.BeginPasskeyLoginResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.BeginPasskeyLogin is not implemented"))
}

func (UnimplementedIdentityServiceHandler) FinishPasskeyLogin(context.Context, *connect.Request[v1.FinishPasskeyLoginRequest]) (*connect.Response[v1.FinishPasskeyLoginResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.FinishPasskeyLogin is not implemented"))
}

func (UnimplementedIdentityServiceHandler) ListPasskeys(context.Context, *connect.Request[v1.ListPasskeysRequest]) (*connect.Response[v1.ListPasskeysResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.ListPasskeys is not implemented"))
}

func (UnimplementedIdentityServiceHandler) RemovePasskey(context.Context, *connect.Request[v1.RemovePasskeyRequest]) (*connect.Response[v1.RemovePasskeyResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.RemovePasskey is not implemented"))
}

func (UnimplementedIdentityServiceHandler) ListFederatedProviders(context.Context, *connect.Request[v1.ListFederatedProvidersRequest]) (*connect.Response[v1.ListFederatedProvidersResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.ListFederatedProviders is not implemented"))
}

func (UnimplementedIdentityServiceHandler) BeginFederatedSignIn(context.Context, *connect.Request[v1.BeginFederatedSignInRequest]) (*connect.Response[v1.BeginFederatedSignInResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.BeginFederatedSignIn is not implemented"))
}

func (UnimplementedIdentityServiceHandler) FinishFederatedSignIn(context.Context, *connect.Request[v1.FinishFederatedSignInRequest]) (*connect.Response[v1.FinishFederatedSignInResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.FinishFederatedSignIn is not implemented"))
}

func (UnimplementedIdentityServiceHandler) BeginFederatedLink(context.Context, *connect.Request[v1.BeginFederatedLinkRequest]) (*connect.Response[v1.BeginFederatedLinkResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.BeginFederatedLink is not implemented"))
}

func (UnimplementedIdentityServiceHandler) FinishFederatedLink(context.Context, *connect.Request[v1.FinishFederatedLinkRequest]) (*connect.Response[v1.FinishFederatedLinkResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.FinishFederatedLink is not implemented"))
}

func (UnimplementedIdentityServiceHandler) UnlinkFederatedIdentity(context.Context, *connect.Request[v1.UnlinkFederatedIdentityRequest]) (*connect.Response[v1.UnlinkFederatedIdentityResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.UnlinkFederatedIdentity is not implemented"))
}

func (UnimplementedIdentityServiceHandler) CreateServiceAccount(context.Context, *connect.Request[v1.CreateServiceAccountRequest]) (*connect.Response[v1.CreateServiceAccountResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.CreateServiceAccount is not implemented"))
}

func (UnimplementedIdentityServiceHandler) ListServiceAccounts(context.Context, *connect.Request[v1.ListServiceAccountsRequest]) (*connect.Response[v1.ListServiceAccountsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.ListServiceAccounts is not implemented"))
}

func (UnimplementedIdentityServiceHandler) CreateApiKey(context.Context, *connect.Request[v1.CreateApiKeyRequest]) (*connect.Response[v1.CreateApiKeyResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.CreateApiKey is not implemented"))
}

func (UnimplementedIdentityServiceHandler) RotateApiKey(context.Context, *connect.Request[v1.RotateApiKeyRequest]) (*connect.Response[v1.RotateApiKeyResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.RotateApiKey is not implemented"))
}

func (UnimplementedIdentityServiceHandler) RevokeApiKey(context.Context, *connect.Request[v1.RevokeApiKeyRequest]) (*connect.Response[v1.RevokeApiKeyResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.RevokeApiKey is not implemented"))
}

func (UnimplementedIdentityServiceHandler) ListApiKeys(context.Context, *connect.Request[v1.ListApiKeysRequest]) (*connect.Response[v1.ListApiKeysResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.identity.v1.IdentityService.ListApiKeys is not implemented"))
}
