// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: chronos/notification/v1/notification.proto

// Package notification is the wire contract for a person's OWN notifications:
// the in-app feed, the browser endpoints their notifications are pushed to, and
// the channel toggles they control.
//
// Four properties of this schema are load-bearing. Each is a rule the messages
// below are shaped by rather than a convention they happen to follow.
//
// **No message here names a recipient.** Not one request carries a subject id, a
// user id or an account id, and that is the schema's decision rather than the
// handler's convenience: every RPC acts on the CALLER, whose pseudonym comes
// from the authenticated session and from nowhere else. A field naming the
// recipient would turn `ListNotifications` into somebody else's inbox and
// `MarkNotificationsRead` into a way to dismiss an alert on a victim's screen.
//
// **A preference names a CHANNEL and can never name a class or a template.**
// `SetNotificationPreferencesRequest` carries exactly one repeated field of
// `(channel, enabled)` pairs. There is deliberately no class field, no template
// field and no "mute everything" switch, because a preference that could name a
// class is a preference that could name `NOTIFICATION_CLASS_SECURITY` — and the
// message telling someone their second factor was removed is the one message an
// account takeover must not be able to switch off (NOTIFICATIONS §3). The
// classes a toggle can and cannot reach are REPORTED by
// `GetNotificationPreferencesResponse`, computed from the same predicate the
// dispatcher enforces with, so the published contract and the runtime behaviour
// cannot disagree.
//
// **Nothing personal is here.** A feed item carries a template name and its
// template data, never rendered text and never an address or a name (ADR-002).
// A push subscription carries transport credentials for a browser, which
// identify a device rather than a person, and no response ever returns them
// again.
//
// **Pagination is keyset, never offset** (CONVENTIONS §7). `page_token` is
// opaque, is bound to the exact query and caller it was minted for, and an
// unreadable one is an error rather than a silent restart at page one.
package notificationv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/chronos/chronos-go/gen/proto/chronos/notification/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// NotificationServiceName is the fully-qualified name of the NotificationService service.
	NotificationServiceName = "chronos.notification.v1.NotificationService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// NotificationServiceListNotificationsProcedure is the fully-qualified name of the
	// NotificationService's ListNotifications RPC.
	NotificationServiceListNotificationsProcedure = "/chronos.notification.v1.NotificationService/ListNotifications"
	// NotificationServiceGetUnreadCountProcedure is the fully-qualified name of the
	// NotificationService's GetUnreadCount RPC.
	NotificationServiceGetUnreadCountProcedure = "/chronos.notification.v1.NotificationService/GetUnreadCount"
	// NotificationServiceMarkNotificationsReadProcedure is the fully-qualified name of the
	// NotificationService's MarkNotificationsRead RPC.
	NotificationServiceMarkNotificationsReadProcedure = "/chronos.notification.v1.NotificationService/MarkNotificationsRead"
	// NotificationServiceRegisterPushSubscriptionProcedure is the fully-qualified name of the
	// NotificationService's RegisterPushSubscription RPC.
	NotificationServiceRegisterPushSubscriptionProcedure = "/chronos.notification.v1.NotificationService/RegisterPushSubscription"
	// NotificationServiceRemovePushSubscriptionProcedure is the fully-qualified name of the
	// NotificationService's RemovePushSubscription RPC.
	NotificationServiceRemovePushSubscriptionProcedure = "/chronos.notification.v1.NotificationService/RemovePushSubscription"
	// NotificationServiceGetNotificationPreferencesProcedure is the fully-qualified name of the
	// NotificationService's GetNotificationPreferences RPC.
	NotificationServiceGetNotificationPreferencesProcedure = "/chronos.notification.v1.NotificationService/GetNotificationPreferences"
	// NotificationServiceSetNotificationPreferencesProcedure is the fully-qualified name of the
	// NotificationService's SetNotificationPreferences RPC.
	NotificationServiceSetNotificationPreferencesProcedure = "/chronos.notification.v1.NotificationService/SetNotificationPreferences"
)

// NotificationServiceClient is a client for the chronos.notification.v1.NotificationService
// service.
type NotificationServiceClient interface {
	// ListNotifications returns one page of the caller's in-app feed, newest
	// first.
	//
	// Keyset paginated over `(occurred_at DESC, notification_id DESC)`. Offsets
	// are banned: a notification arriving between two requests shifts every later
	// OFFSET page, so a person walking their inbox would silently miss items.
	ListNotifications(context.Context, *connect.Request[v1.ListNotificationsRequest]) (*connect.Response[v1.ListNotificationsResponse], error)
	// GetUnreadCount returns how many of the caller's notifications are unread.
	//
	// Separate from the list because it is what a badge needs on first paint,
	// before any page has been fetched. It is not a polling endpoint: the realtime
	// stream moves the badge afterwards.
	GetUnreadCount(context.Context, *connect.Request[v1.GetUnreadCountRequest]) (*connect.Response[v1.GetUnreadCountResponse], error)
	// MarkNotificationsRead dismisses items the caller has seen.
	//
	// It appends one `notification.Read.v1` event per item, atomically across
	// every item's stream: a batch either lands whole or not at all, so a client
	// never has to reconcile a half-dismissed screen.
	//
	// Marking is idempotent in the strongest sense available — the FIRST read
	// time is kept, so a replay, a retry and a second device all leave the same
	// answer to "when did they first see this", which is the question email
	// arbitration asks (ADR-026).
	MarkNotificationsRead(context.Context, *connect.Request[v1.MarkNotificationsReadRequest]) (*connect.Response[v1.MarkNotificationsReadResponse], error)
	// RegisterPushSubscription enrols one browser profile on one device.
	//
	// Re-registering the same browser in the same organization is the NORMAL case,
	// not an error: a permission prompt answered again produces the same endpoint,
	// and this collapses onto the existing subscription rather than creating a
	// second one that would push to that device twice.
	RegisterPushSubscription(context.Context, *connect.Request[v1.RegisterPushSubscriptionRequest]) (*connect.Response[v1.RegisterPushSubscriptionResponse], error)
	// RemovePushSubscription retires a browser endpoint.
	//
	// The row is marked expired rather than deleted, because "why did I stop
	// getting push?" is a real support question and a deleted row cannot answer
	// it. Removing an endpoint that was never registered is not an error: the
	// caller wanted it gone, and it is gone.
	RemovePushSubscription(context.Context, *connect.Request[v1.RemovePushSubscriptionRequest]) (*connect.Response[v1.RemovePushSubscriptionResponse], error)
	// GetNotificationPreferences returns the caller's own channel toggles, and
	// what those toggles reach.
	GetNotificationPreferences(context.Context, *connect.Request[v1.GetNotificationPreferencesRequest]) (*connect.Response[v1.GetNotificationPreferencesResponse], error)
	// SetNotificationPreferences changes the caller's own channel toggles.
	//
	// It appends `notification.ChannelPreferenceSet.v1` and never writes the
	// preference table: that table is a projection, and a handler writing it
	// directly would put state in PostgreSQL that no replay reproduces and the
	// next rebuild deletes.
	//
	// Concurrent saves are serialised by the preference stream's own revision, so
	// two settings screens saving at once produce one ordering rather than a torn
	// mixture of both.
	SetNotificationPreferences(context.Context, *connect.Request[v1.SetNotificationPreferencesRequest]) (*connect.Response[v1.SetNotificationPreferencesResponse], error)
}

// NewNotificationServiceClient constructs a client for the
// chronos.notification.v1.NotificationService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewNotificationServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) NotificationServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	notificationServiceMethods := v1.File_chronos_notification_v1_notification_proto.Services().ByName("NotificationService").Methods()
	return &notificationServiceClient{
		listNotifications: connect.NewClient[v1.ListNotificationsRequest, v1.ListNotificationsResponse](
			httpClient,
			baseURL+NotificationServiceListNotificationsProcedure,
			connect.WithSchema(notificationServiceMethods.ByName("ListNotifications")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		getUnreadCount: connect.NewClient[v1.GetUnreadCountRequest, v1.GetUnreadCountResponse](
			httpClient,
			baseURL+NotificationServiceGetUnreadCountProcedure,
			connect.WithSchema(notificationServiceMethods.ByName("GetUnreadCount")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		markNotificationsRead: connect.NewClient[v1.MarkNotificationsReadRequest, v1.MarkNotificationsReadResponse](
			httpClient,
			baseURL+NotificationServiceMarkNotificationsReadProcedure,
			connect.WithSchema(notificationServiceMethods.ByName("MarkNotificationsRead")),
			connect.WithClientOptions(opts...),
		),
		registerPushSubscription: connect.NewClient[v1.RegisterPushSubscriptionRequest, v1.RegisterPushSubscriptionResponse](
			httpClient,
			baseURL+NotificationServiceRegisterPushSubscriptionProcedure,
			connect.WithSchema(notificationServiceMethods.ByName("RegisterPushSubscription")),
			connect.WithClientOptions(opts...),
		),
		removePushSubscription: connect.NewClient[v1.RemovePushSubscriptionRequest, v1.RemovePushSubscriptionResponse](
			httpClient,
			baseURL+NotificationServiceRemovePushSubscriptionProcedure,
			connect.WithSchema(notificationServiceMethods.ByName("RemovePushSubscription")),
			connect.WithClientOptions(opts...),
		),
		getNotificationPreferences: connect.NewClient[v1.GetNotificationPreferencesRequest, v1.GetNotificationPreferencesResponse](
			httpClient,
			baseURL+NotificationServiceGetNotificationPreferencesProcedure,
			connect.WithSchema(notificationServiceMethods.ByName("GetNotificationPreferences")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		setNotificationPreferences: connect.NewClient[v1.SetNotificationPreferencesRequest, v1.SetNotificationPreferencesResponse](
			httpClient,
			baseURL+NotificationServiceSetNotificationPreferencesProcedure,
			connect.WithSchema(notificationServiceMethods.ByName("SetNotificationPreferences")),
			connect.WithClientOptions(opts...),
		),
	}
}

// notificationServiceClient implements NotificationServiceClient.
type notificationServiceClient struct {
	listNotifications          *connect.Client[v1.ListNotificationsRequest, v1.ListNotificationsResponse]
	getUnreadCount             *connect.Client[v1.GetUnreadCountRequest, v1.GetUnreadCountResponse]
	markNotificationsRead      *connect.Client[v1.MarkNotificationsReadRequest, v1.MarkNotificationsReadResponse]
	registerPushSubscription   *connect.Client[v1.RegisterPushSubscriptionRequest, v1.RegisterPushSubscriptionResponse]
	removePushSubscription     *connect.Client[v1.RemovePushSubscriptionRequest, v1.RemovePushSubscriptionResponse]
	getNotificationPreferences *connect.Client[v1.GetNotificationPreferencesRequest, v1.GetNotificationPreferencesResponse]
	setNotificationPreferences *connect.Client[v1.SetNotificationPreferencesRequest, v1.SetNotificationPreferencesResponse]
}

// ListNotifications calls chronos.notification.v1.NotificationService.ListNotifications.
func (c *notificationServiceClient) ListNotifications(ctx context.Context, req *connect.Request[v1.ListNotificationsRequest]) (*connect.Response[v1.ListNotificationsResponse], error) {
	return c.listNotifications.CallUnary(ctx, req)
}

// GetUnreadCount calls chronos.notification.v1.NotificationService.GetUnreadCount.
func (c *notificationServiceClient) GetUnreadCount(ctx context.Context, req *connect.Request[v1.GetUnreadCountRequest]) (*connect.Response[v1.GetUnreadCountResponse], error) {
	return c.getUnreadCount.CallUnary(ctx, req)
}

// MarkNotificationsRead calls chronos.notification.v1.NotificationService.MarkNotificationsRead.
func (c *notificationServiceClient) MarkNotificationsRead(ctx context.Context, req *connect.Request[v1.MarkNotificationsReadRequest]) (*connect.Response[v1.MarkNotificationsReadResponse], error) {
	return c.markNotificationsRead.CallUnary(ctx, req)
}

// RegisterPushSubscription calls
// chronos.notification.v1.NotificationService.RegisterPushSubscription.
func (c *notificationServiceClient) RegisterPushSubscription(ctx context.Context, req *connect.Request[v1.RegisterPushSubscriptionRequest]) (*connect.Response[v1.RegisterPushSubscriptionResponse], error) {
	return c.registerPushSubscription.CallUnary(ctx, req)
}

// RemovePushSubscription calls chronos.notification.v1.NotificationService.RemovePushSubscription.
func (c *notificationServiceClient) RemovePushSubscription(ctx context.Context, req *connect.Request[v1.RemovePushSubscriptionRequest]) (*connect.Response[v1.RemovePushSubscriptionResponse], error) {
	return c.removePushSubscription.CallUnary(ctx, req)
}

// GetNotificationPreferences calls
// chronos.notification.v1.NotificationService.GetNotificationPreferences.
func (c *notificationServiceClient) GetNotificationPreferences(ctx context.Context, req *connect.Request[v1.GetNotificationPreferencesRequest]) (*connect.Response[v1.GetNotificationPreferencesResponse], error) {
	return c.getNotificationPreferences.CallUnary(ctx, req)
}

// SetNotificationPreferences calls
// chronos.notification.v1.NotificationService.SetNotificationPreferences.
func (c *notificationServiceClient) SetNotificationPreferences(ctx context.Context, req *connect.Request[v1.SetNotificationPreferencesRequest]) (*connect.Response[v1.SetNotificationPreferencesResponse], error) {
	return c.setNotificationPreferences.CallUnary(ctx, req)
}

// NotificationServiceHandler is an implementation of the
// chronos.notification.v1.NotificationService service.
type NotificationServiceHandler interface {
	// ListNotifications returns one page of the caller's in-app feed, newest
	// first.
	//
	// Keyset paginated over `(occurred_at DESC, notification_id DESC)`. Offsets
	// are banned: a notification arriving between two requests shifts every later
	// OFFSET page, so a person walking their inbox would silently miss items.
	ListNotifications(context.Context, *connect.Request[v1.ListNotificationsRequest]) (*connect.Response[v1.ListNotificationsResponse], error)
	// GetUnreadCount returns how many of the caller's notifications are unread.
	//
	// Separate from the list because it is what a badge needs on first paint,
	// before any page has been fetched. It is not a polling endpoint: the realtime
	// stream moves the badge afterwards.
	GetUnreadCount(context.Context, *connect.Request[v1.GetUnreadCountRequest]) (*connect.Response[v1.GetUnreadCountResponse], error)
	// MarkNotificationsRead dismisses items the caller has seen.
	//
	// It appends one `notification.Read.v1` event per item, atomically across
	// every item's stream: a batch either lands whole or not at all, so a client
	// never has to reconcile a half-dismissed screen.
	//
	// Marking is idempotent in the strongest sense available — the FIRST read
	// time is kept, so a replay, a retry and a second device all leave the same
	// answer to "when did they first see this", which is the question email
	// arbitration asks (ADR-026).
	MarkNotificationsRead(context.Context, *connect.Request[v1.MarkNotificationsReadRequest]) (*connect.Response[v1.MarkNotificationsReadResponse], error)
	// RegisterPushSubscription enrols one browser profile on one device.
	//
	// Re-registering the same browser in the same organization is the NORMAL case,
	// not an error: a permission prompt answered again produces the same endpoint,
	// and this collapses onto the existing subscription rather than creating a
	// second one that would push to that device twice.
	RegisterPushSubscription(context.Context, *connect.Request[v1.RegisterPushSubscriptionRequest]) (*connect.Response[v1.RegisterPushSubscriptionResponse], error)
	// RemovePushSubscription retires a browser endpoint.
	//
	// The row is marked expired rather than deleted, because "why did I stop
	// getting push?" is a real support question and a deleted row cannot answer
	// it. Removing an endpoint that was never registered is not an error: the
	// caller wanted it gone, and it is gone.
	RemovePushSubscription(context.Context, *connect.Request[v1.RemovePushSubscriptionRequest]) (*connect.Response[v1.RemovePushSubscriptionResponse], error)
	// GetNotificationPreferences returns the caller's own channel toggles, and
	// what those toggles reach.
	GetNotificationPreferences(context.Context, *connect.Request[v1.GetNotificationPreferencesRequest]) (*connect.Response[v1.GetNotificationPreferencesResponse], error)
	// SetNotificationPreferences changes the caller's own channel toggles.
	//
	// It appends `notification.ChannelPreferenceSet.v1` and never writes the
	// preference table: that table is a projection, and a handler writing it
	// directly would put state in PostgreSQL that no replay reproduces and the
	// next rebuild deletes.
	//
	// Concurrent saves are serialised by the preference stream's own revision, so
	// two settings screens saving at once produce one ordering rather than a torn
	// mixture of both.
	SetNotificationPreferences(context.Context, *connect.Request[v1.SetNotificationPreferencesRequest]) (*connect.Response[v1.SetNotificationPreferencesResponse], error)
}

// NewNotificationServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewNotificationServiceHandler(svc NotificationServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	notificationServiceMethods := v1.File_chronos_notification_v1_notification_proto.Services().ByName("NotificationService").Methods()
	notificationServiceListNotificationsHandler := connect.NewUnaryHandler(
		NotificationServiceListNotificationsProcedure,
		svc.ListNotifications,
		connect.WithSchema(notificationServiceMethods.ByName("ListNotifications")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	notificationServiceGetUnreadCountHandler := connect.NewUnaryHandler(
		NotificationServiceGetUnreadCountProcedure,
		svc.GetUnreadCount,
		connect.WithSchema(notificationServiceMethods.ByName("GetUnreadCount")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	notificationServiceMarkNotificationsReadHandler := connect.NewUnaryHandler(
		NotificationServiceMarkNotificationsReadProcedure,
		svc.MarkNotificationsRead,
		connect.WithSchema(notificationServiceMethods.ByName("MarkNotificationsRead")),
		connect.WithHandlerOptions(opts...),
	)
	notificationServiceRegisterPushSubscriptionHandler := connect.NewUnaryHandler(
		NotificationServiceRegisterPushSubscriptionProcedure,
		svc.RegisterPushSubscription,
		connect.WithSchema(notificationServiceMethods.ByName("RegisterPushSubscription")),
		connect.WithHandlerOptions(opts...),
	)
	notificationServiceRemovePushSubscriptionHandler := connect.NewUnaryHandler(
		NotificationServiceRemovePushSubscriptionProcedure,
		svc.RemovePushSubscription,
		connect.WithSchema(notificationServiceMethods.ByName("RemovePushSubscription")),
		connect.WithHandlerOptions(opts...),
	)
	notificationServiceGetNotificationPreferencesHandler := connect.NewUnaryHandler(
		NotificationServiceGetNotificationPreferencesProcedure,
		svc.GetNotificationPreferences,
		connect.WithSchema(notificationServiceMethods.ByName("GetNotificationPreferences")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	notificationServiceSetNotificationPreferencesHandler := connect.NewUnaryHandler(
		NotificationServiceSetNotificationPreferencesProcedure,
		svc.SetNotificationPreferences,
		connect.WithSchema(notificationServiceMethods.ByName("SetNotificationPreferences")),
		connect.WithHandlerOptions(opts...),
	)
	return "/chronos.notification.v1.NotificationService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case NotificationServiceListNotificationsProcedure:
			notificationServiceListNotificationsHandler.ServeHTTP(w, r)
		case NotificationServiceGetUnreadCountProcedure:
			notificationServiceGetUnreadCountHandler.ServeHTTP(w, r)
		case NotificationServiceMarkNotificationsReadProcedure:
			notificationServiceMarkNotificationsReadHandler.ServeHTTP(w, r)
		case NotificationServiceRegisterPushSubscriptionProcedure:
			notificationServiceRegisterPushSubscriptionHandler.ServeHTTP(w, r)
		case NotificationServiceRemovePushSubscriptionProcedure:
			notificationServiceRemovePushSubscriptionHandler.ServeHTTP(w, r)
		case NotificationServiceGetNotificationPreferencesProcedure:
			notificationServiceGetNotificationPreferencesHandler.ServeHTTP(w, r)
		case NotificationServiceSetNotificationPreferencesProcedure:
			notificationServiceSetNotificationPreferencesHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedNotificationServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedNotificationServiceHandler struct{}

func (UnimplementedNotificationServiceHandler) ListNotifications(context.Context, *connect.Request[v1.ListNotificationsRequest]) (*connect.Response[v1.ListNotificationsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.notification.v1.NotificationService.ListNotifications is not implemented"))
}

func (UnimplementedNotificationServiceHandler) GetUnreadCount(context.Context, *connect.Request[v1.GetUnreadCountRequest]) (*connect.Response[v1.GetUnreadCountResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.notification.v1.NotificationService.GetUnreadCount is not implemented"))
}

func (UnimplementedNotificationServiceHandler) MarkNotificationsRead(context.Context, *connect.Request[v1.MarkNotificationsReadRequest]) (*connect.Response[v1.MarkNotificationsReadResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.notification.v1.NotificationService.MarkNotificationsRead is not implemented"))
}

func (UnimplementedNotificationServiceHandler) RegisterPushSubscription(context.Context, *connect.Request[v1.RegisterPushSubscriptionRequest]) (*connect.Response[v1.RegisterPushSubscriptionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.notification.v1.NotificationService.RegisterPushSubscription is not implemented"))
}

func (UnimplementedNotificationServiceHandler) RemovePushSubscription(context.Context, *connect.Request[v1.RemovePushSubscriptionRequest]) (*connect.Response[v1.RemovePushSubscriptionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.notification.v1.NotificationService.RemovePushSubscription is not implemented"))
}

func (UnimplementedNotificationServiceHandler) GetNotificationPreferences(context.Context, *connect.Request[v1.GetNotificationPreferencesRequest]) (*connect.Response[v1.GetNotificationPreferencesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.notification.v1.NotificationService.GetNotificationPreferences is not implemented"))
}

func (UnimplementedNotificationServiceHandler) SetNotificationPreferences(context.Context, *connect.Request[v1.SetNotificationPreferencesRequest]) (*connect.Response[v1.SetNotificationPreferencesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.notification.v1.NotificationService.SetNotificationPreferences is not implemented"))
}
