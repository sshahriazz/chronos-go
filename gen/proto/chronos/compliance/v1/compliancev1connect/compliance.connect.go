// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: chronos/compliance/v1/compliance.proto

// Package compliance is the wire contract for a data subject's own rights.
//
// Every method here acts on the CALLER'S OWN data and names no subject. That is
// not a convenience: a request that could name a subject is a request to
// exercise somebody else's rights, and the most dangerous endpoint in a product
// like this is one that acts on a person named by whoever asked.
//
// The rights are Chapter III of the GDPR. What is here is what is built;
// compliance.md §3 lists six and this carries one, because shipping an endpoint
// for a right the system cannot actually deliver is worse than not having it.
package compliancev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/chronos/chronos-go/gen/proto/chronos/compliance/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ComplianceServiceName is the fully-qualified name of the ComplianceService service.
	ComplianceServiceName = "chronos.compliance.v1.ComplianceService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ComplianceServiceRestrictProcessingProcedure is the fully-qualified name of the
	// ComplianceService's RestrictProcessing RPC.
	ComplianceServiceRestrictProcessingProcedure = "/chronos.compliance.v1.ComplianceService/RestrictProcessing"
	// ComplianceServiceLiftProcessingRestrictionProcedure is the fully-qualified name of the
	// ComplianceService's LiftProcessingRestriction RPC.
	ComplianceServiceLiftProcessingRestrictionProcedure = "/chronos.compliance.v1.ComplianceService/LiftProcessingRestriction"
	// ComplianceServiceGetProcessingRestrictionProcedure is the fully-qualified name of the
	// ComplianceService's GetProcessingRestriction RPC.
	ComplianceServiceGetProcessingRestrictionProcedure = "/chronos.compliance.v1.ComplianceService/GetProcessingRestriction"
)

// ComplianceServiceClient is a client for the chronos.compliance.v1.ComplianceService service.
type ComplianceServiceClient interface {
	// RestrictProcessing halts processing of the caller's data (Article 18).
	//
	// # What it stops, and what it deliberately does not
	//
	// Storage CONTINUES and nothing is deleted — that is the whole distinction
	// from erasure. The account keeps working, sign-in keeps working, projections
	// keep building. What stops is being contacted: no transactional mail, no
	// activity nudges, nothing product.
	//
	// SECURITY ALERTS STILL ARRIVE, and that exemption is deliberate. A control a
	// session holder can set, which stops a security alert, is a control an
	// attacker sets to silence the message that would reveal them — the same
	// reason a notification preference cannot switch one off. Article 18(2)
	// permits it: restriction does not bar processing needed to protect the
	// rights of a natural person, and a warning to the account's own holder about
	// their own account is exactly that.
	//
	// # Why AAL2
	//
	// Restricting somebody silences most of what would tell them their account is
	// being used. Requiring step-up does not remove that risk — the security
	// exemption above is what addresses it — but an endpoint that quietens a
	// person's mail must not be reachable from a session that has proven one
	// factor.
	//
	// Idempotent: a second call reports `changed: false` and returns the ORIGINAL
	// instant.
	RestrictProcessing(context.Context, *connect.Request[v1.RestrictProcessingRequest]) (*connect.Response[v1.RestrictProcessingResponse], error)
	// LiftProcessingRestriction resumes processing of the caller's data.
	//
	// Nothing was lost while the restriction stood, which is Article 18's point as
	// distinct from Article 17: the data was held and not processed, so resuming
	// needs no recovery.
	//
	// Lifting nothing succeeds and reports `changed: false`.
	LiftProcessingRestriction(context.Context, *connect.Request[v1.LiftProcessingRestrictionRequest]) (*connect.Response[v1.LiftProcessingRestrictionResponse], error)
	// GetProcessingRestriction reports whether the caller's data is restricted.
	//
	// A READ, and AAL1: somebody must be able to see the state of their own
	// request from an ordinary session. Requiring step-up to read what you already
	// asked for would make the setting hard to verify and easy to forget.
	GetProcessingRestriction(context.Context, *connect.Request[v1.GetProcessingRestrictionRequest]) (*connect.Response[v1.GetProcessingRestrictionResponse], error)
}

// NewComplianceServiceClient constructs a client for the chronos.compliance.v1.ComplianceService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewComplianceServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ComplianceServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	complianceServiceMethods := v1.File_chronos_compliance_v1_compliance_proto.Services().ByName("ComplianceService").Methods()
	return &complianceServiceClient{
		restrictProcessing: connect.NewClient[v1.RestrictProcessingRequest, v1.RestrictProcessingResponse](
			httpClient,
			baseURL+ComplianceServiceRestrictProcessingProcedure,
			connect.WithSchema(complianceServiceMethods.ByName("RestrictProcessing")),
			connect.WithClientOptions(opts...),
		),
		liftProcessingRestriction: connect.NewClient[v1.LiftProcessingRestrictionRequest, v1.LiftProcessingRestrictionResponse](
			httpClient,
			baseURL+ComplianceServiceLiftProcessingRestrictionProcedure,
			connect.WithSchema(complianceServiceMethods.ByName("LiftProcessingRestriction")),
			connect.WithClientOptions(opts...),
		),
		getProcessingRestriction: connect.NewClient[v1.GetProcessingRestrictionRequest, v1.GetProcessingRestrictionResponse](
			httpClient,
			baseURL+ComplianceServiceGetProcessingRestrictionProcedure,
			connect.WithSchema(complianceServiceMethods.ByName("GetProcessingRestriction")),
			connect.WithClientOptions(opts...),
		),
	}
}

// complianceServiceClient implements ComplianceServiceClient.
type complianceServiceClient struct {
	restrictProcessing        *connect.Client[v1.RestrictProcessingRequest, v1.RestrictProcessingResponse]
	liftProcessingRestriction *connect.Client[v1.LiftProcessingRestrictionRequest, v1.LiftProcessingRestrictionResponse]
	getProcessingRestriction  *connect.Client[v1.GetProcessingRestrictionRequest, v1.GetProcessingRestrictionResponse]
}

// RestrictProcessing calls chronos.compliance.v1.ComplianceService.RestrictProcessing.
func (c *complianceServiceClient) RestrictProcessing(ctx context.Context, req *connect.Request[v1.RestrictProcessingRequest]) (*connect.Response[v1.RestrictProcessingResponse], error) {
	return c.restrictProcessing.CallUnary(ctx, req)
}

// LiftProcessingRestriction calls
// chronos.compliance.v1.ComplianceService.LiftProcessingRestriction.
func (c *complianceServiceClient) LiftProcessingRestriction(ctx context.Context, req *connect.Request[v1.LiftProcessingRestrictionRequest]) (*connect.Response[v1.LiftProcessingRestrictionResponse], error) {
	return c.liftProcessingRestriction.CallUnary(ctx, req)
}

// GetProcessingRestriction calls chronos.compliance.v1.ComplianceService.GetProcessingRestriction.
func (c *complianceServiceClient) GetProcessingRestriction(ctx context.Context, req *connect.Request[v1.GetProcessingRestrictionRequest]) (*connect.Response[v1.GetProcessingRestrictionResponse], error) {
	return c.getProcessingRestriction.CallUnary(ctx, req)
}

// ComplianceServiceHandler is an implementation of the chronos.compliance.v1.ComplianceService
// service.
type ComplianceServiceHandler interface {
	// RestrictProcessing halts processing of the caller's data (Article 18).
	//
	// # What it stops, and what it deliberately does not
	//
	// Storage CONTINUES and nothing is deleted — that is the whole distinction
	// from erasure. The account keeps working, sign-in keeps working, projections
	// keep building. What stops is being contacted: no transactional mail, no
	// activity nudges, nothing product.
	//
	// SECURITY ALERTS STILL ARRIVE, and that exemption is deliberate. A control a
	// session holder can set, which stops a security alert, is a control an
	// attacker sets to silence the message that would reveal them — the same
	// reason a notification preference cannot switch one off. Article 18(2)
	// permits it: restriction does not bar processing needed to protect the
	// rights of a natural person, and a warning to the account's own holder about
	// their own account is exactly that.
	//
	// # Why AAL2
	//
	// Restricting somebody silences most of what would tell them their account is
	// being used. Requiring step-up does not remove that risk — the security
	// exemption above is what addresses it — but an endpoint that quietens a
	// person's mail must not be reachable from a session that has proven one
	// factor.
	//
	// Idempotent: a second call reports `changed: false` and returns the ORIGINAL
	// instant.
	RestrictProcessing(context.Context, *connect.Request[v1.RestrictProcessingRequest]) (*connect.Response[v1.RestrictProcessingResponse], error)
	// LiftProcessingRestriction resumes processing of the caller's data.
	//
	// Nothing was lost while the restriction stood, which is Article 18's point as
	// distinct from Article 17: the data was held and not processed, so resuming
	// needs no recovery.
	//
	// Lifting nothing succeeds and reports `changed: false`.
	LiftProcessingRestriction(context.Context, *connect.Request[v1.LiftProcessingRestrictionRequest]) (*connect.Response[v1.LiftProcessingRestrictionResponse], error)
	// GetProcessingRestriction reports whether the caller's data is restricted.
	//
	// A READ, and AAL1: somebody must be able to see the state of their own
	// request from an ordinary session. Requiring step-up to read what you already
	// asked for would make the setting hard to verify and easy to forget.
	GetProcessingRestriction(context.Context, *connect.Request[v1.GetProcessingRestrictionRequest]) (*connect.Response[v1.GetProcessingRestrictionResponse], error)
}

// NewComplianceServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewComplianceServiceHandler(svc ComplianceServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	complianceServiceMethods := v1.File_chronos_compliance_v1_compliance_proto.Services().ByName("ComplianceService").Methods()
	complianceServiceRestrictProcessingHandler := connect.NewUnaryHandler(
		ComplianceServiceRestrictProcessingProcedure,
		svc.RestrictProcessing,
		connect.WithSchema(complianceServiceMethods.ByName("RestrictProcessing")),
		connect.WithHandlerOptions(opts...),
	)
	complianceServiceLiftProcessingRestrictionHandler := connect.NewUnaryHandler(
		ComplianceServiceLiftProcessingRestrictionProcedure,
		svc.LiftProcessingRestriction,
		connect.WithSchema(complianceServiceMethods.ByName("LiftProcessingRestriction")),
		connect.WithHandlerOptions(opts...),
	)
	complianceServiceGetProcessingRestrictionHandler := connect.NewUnaryHandler(
		ComplianceServiceGetProcessingRestrictionProcedure,
		svc.GetProcessingRestriction,
		connect.WithSchema(complianceServiceMethods.ByName("GetProcessingRestriction")),
		connect.WithHandlerOptions(opts...),
	)
	return "/chronos.compliance.v1.ComplianceService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ComplianceServiceRestrictProcessingProcedure:
			complianceServiceRestrictProcessingHandler.ServeHTTP(w, r)
		case ComplianceServiceLiftProcessingRestrictionProcedure:
			complianceServiceLiftProcessingRestrictionHandler.ServeHTTP(w, r)
		case ComplianceServiceGetProcessingRestrictionProcedure:
			complianceServiceGetProcessingRestrictionHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedComplianceServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedComplianceServiceHandler struct{}

func (UnimplementedComplianceServiceHandler) RestrictProcessing(context.Context, *connect.Request[v1.RestrictProcessingRequest]) (*connect.Response[v1.RestrictProcessingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.compliance.v1.ComplianceService.RestrictProcessing is not implemented"))
}

func (UnimplementedComplianceServiceHandler) LiftProcessingRestriction(context.Context, *connect.Request[v1.LiftProcessingRestrictionRequest]) (*connect.Response[v1.LiftProcessingRestrictionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.compliance.v1.ComplianceService.LiftProcessingRestriction is not implemented"))
}

func (UnimplementedComplianceServiceHandler) GetProcessingRestriction(context.Context, *connect.Request[v1.GetProcessingRestrictionRequest]) (*connect.Response[v1.GetProcessingRestrictionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.compliance.v1.ComplianceService.GetProcessingRestriction is not implemented"))
}
