// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: chronos/system/v1/system.proto

// Package system exposes the server's own state — the probe surface the
// frontend uses to degrade deliberately rather than showing failed requests
// (ADR-010).
package systemv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/chronos/chronos-go/gen/proto/chronos/system/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SystemServiceName is the fully-qualified name of the SystemService service.
	SystemServiceName = "chronos.system.v1.SystemService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SystemServiceGetStatusProcedure is the fully-qualified name of the SystemService's GetStatus RPC.
	SystemServiceGetStatusProcedure = "/chronos.system.v1.SystemService/GetStatus"
)

// SystemServiceClient is a client for the chronos.system.v1.SystemService service.
type SystemServiceClient interface {
	// GetStatus reports per-dependency state so a client can grey out affected
	// features instead of discovering them through failed calls.
	//
	// Public by design: it must answer while the system is degraded, including
	// when authentication itself is unavailable.
	GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error)
}

// NewSystemServiceClient constructs a client for the chronos.system.v1.SystemService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSystemServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SystemServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	systemServiceMethods := v1.File_chronos_system_v1_system_proto.Services().ByName("SystemService").Methods()
	return &systemServiceClient{
		getStatus: connect.NewClient[v1.GetStatusRequest, v1.GetStatusResponse](
			httpClient,
			baseURL+SystemServiceGetStatusProcedure,
			connect.WithSchema(systemServiceMethods.ByName("GetStatus")),
			connect.WithClientOptions(opts...),
		),
	}
}

// systemServiceClient implements SystemServiceClient.
type systemServiceClient struct {
	getStatus *connect.Client[v1.GetStatusRequest, v1.GetStatusResponse]
}

// GetStatus calls chronos.system.v1.SystemService.GetStatus.
func (c *systemServiceClient) GetStatus(ctx context.Context, req *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error) {
	return c.getStatus.CallUnary(ctx, req)
}

// SystemServiceHandler is an implementation of the chronos.system.v1.SystemService service.
type SystemServiceHandler interface {
	// GetStatus reports per-dependency state so a client can grey out affected
	// features instead of discovering them through failed calls.
	//
	// Public by design: it must answer while the system is degraded, including
	// when authentication itself is unavailable.
	GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error)
}

// NewSystemServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSystemServiceHandler(svc SystemServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	systemServiceMethods := v1.File_chronos_system_v1_system_proto.Services().ByName("SystemService").Methods()
	systemServiceGetStatusHandler := connect.NewUnaryHandler(
		SystemServiceGetStatusProcedure,
		svc.GetStatus,
		connect.WithSchema(systemServiceMethods.ByName("GetStatus")),
		connect.WithHandlerOptions(opts...),
	)
	return "/chronos.system.v1.SystemService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SystemServiceGetStatusProcedure:
			systemServiceGetStatusHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSystemServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSystemServiceHandler struct{}

func (UnimplementedSystemServiceHandler) GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.system.v1.SystemService.GetStatus is not implemented"))
}
