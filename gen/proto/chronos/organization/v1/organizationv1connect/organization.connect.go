// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: chronos/organization/v1/organization.proto

// Package organization is the wire contract for the commercial boundary: one
// organization, one customer contract, one subscription.
//
// Three properties of this schema are load-bearing.
//
// **An organization is the tenant, and a person owns at most one.** That is
// organization.md §1's invariant, and it is enforced by an atomic append to a
// reservation stream named after the owner — not by a uniqueness check on a
// projection, which is behind the log by construction and would let two
// concurrent creations both succeed (ADR-052).
//
// **Creation does not require payment.** The trial is cardless: an organization
// is created, provisioned with a Stripe subscription by a reactor, and starts
// trialing. `provisional_owner` and `PendingActivation` — which organization.md
// §4 introduces to let an unpaid creator reach checkout — do not exist here,
// because a working trial removes the problem they solve.
//
// **The creator is the owner from the first event.** There is no field naming an
// owner: it is the authenticated caller, and nothing else. A request that could
// name its own owner is a request that can create an organization belonging to
// somebody else.
package organizationv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/chronos/chronos-go/gen/proto/chronos/organization/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// OrganizationServiceName is the fully-qualified name of the OrganizationService service.
	OrganizationServiceName = "chronos.organization.v1.OrganizationService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// OrganizationServiceCreateOrganizationProcedure is the fully-qualified name of the
	// OrganizationService's CreateOrganization RPC.
	OrganizationServiceCreateOrganizationProcedure = "/chronos.organization.v1.OrganizationService/CreateOrganization"
)

// OrganizationServiceClient is a client for the chronos.organization.v1.OrganizationService
// service.
type OrganizationServiceClient interface {
	// CreateOrganization opens an organization owned by the caller.
	//
	// # What makes this safe to retry
	//
	// The organization, the owner reservation and the slug reservation are written
	// in ONE atomic append, each with a `NoStream` precondition. A retry with the
	// same `Idempotency-Key` derives byte-identical event ids and is refused as a
	// duplicate; a genuine second attempt loses the precondition and is refused as
	// a `CONFLICT`. Neither leaves a half-created organization behind, because a
	// failed multi-append writes nothing at all.
	//
	// # Why it is self-scoped
	//
	// There is no organization to check a permission against — this call is what
	// brings one into existence. The only authorization question is "is there an
	// authenticated person here", which the `self` relation answers locally
	// without consulting the graph.
	//
	// A person may own ONE organization. A second attempt returns `CONFLICT`.
	CreateOrganization(context.Context, *connect.Request[v1.CreateOrganizationRequest]) (*connect.Response[v1.CreateOrganizationResponse], error)
}

// NewOrganizationServiceClient constructs a client for the
// chronos.organization.v1.OrganizationService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewOrganizationServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) OrganizationServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	organizationServiceMethods := v1.File_chronos_organization_v1_organization_proto.Services().ByName("OrganizationService").Methods()
	return &organizationServiceClient{
		createOrganization: connect.NewClient[v1.CreateOrganizationRequest, v1.CreateOrganizationResponse](
			httpClient,
			baseURL+OrganizationServiceCreateOrganizationProcedure,
			connect.WithSchema(organizationServiceMethods.ByName("CreateOrganization")),
			connect.WithClientOptions(opts...),
		),
	}
}

// organizationServiceClient implements OrganizationServiceClient.
type organizationServiceClient struct {
	createOrganization *connect.Client[v1.CreateOrganizationRequest, v1.CreateOrganizationResponse]
}

// CreateOrganization calls chronos.organization.v1.OrganizationService.CreateOrganization.
func (c *organizationServiceClient) CreateOrganization(ctx context.Context, req *connect.Request[v1.CreateOrganizationRequest]) (*connect.Response[v1.CreateOrganizationResponse], error) {
	return c.createOrganization.CallUnary(ctx, req)
}

// OrganizationServiceHandler is an implementation of the
// chronos.organization.v1.OrganizationService service.
type OrganizationServiceHandler interface {
	// CreateOrganization opens an organization owned by the caller.
	//
	// # What makes this safe to retry
	//
	// The organization, the owner reservation and the slug reservation are written
	// in ONE atomic append, each with a `NoStream` precondition. A retry with the
	// same `Idempotency-Key` derives byte-identical event ids and is refused as a
	// duplicate; a genuine second attempt loses the precondition and is refused as
	// a `CONFLICT`. Neither leaves a half-created organization behind, because a
	// failed multi-append writes nothing at all.
	//
	// # Why it is self-scoped
	//
	// There is no organization to check a permission against — this call is what
	// brings one into existence. The only authorization question is "is there an
	// authenticated person here", which the `self` relation answers locally
	// without consulting the graph.
	//
	// A person may own ONE organization. A second attempt returns `CONFLICT`.
	CreateOrganization(context.Context, *connect.Request[v1.CreateOrganizationRequest]) (*connect.Response[v1.CreateOrganizationResponse], error)
}

// NewOrganizationServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewOrganizationServiceHandler(svc OrganizationServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	organizationServiceMethods := v1.File_chronos_organization_v1_organization_proto.Services().ByName("OrganizationService").Methods()
	organizationServiceCreateOrganizationHandler := connect.NewUnaryHandler(
		OrganizationServiceCreateOrganizationProcedure,
		svc.CreateOrganization,
		connect.WithSchema(organizationServiceMethods.ByName("CreateOrganization")),
		connect.WithHandlerOptions(opts...),
	)
	return "/chronos.organization.v1.OrganizationService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case OrganizationServiceCreateOrganizationProcedure:
			organizationServiceCreateOrganizationHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedOrganizationServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedOrganizationServiceHandler struct{}

func (UnimplementedOrganizationServiceHandler) CreateOrganization(context.Context, *connect.Request[v1.CreateOrganizationRequest]) (*connect.Response[v1.CreateOrganizationResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.organization.v1.OrganizationService.CreateOrganization is not implemented"))
}
