// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: chronos/profile/v1/profile.proto

// Package profile is the wire contract for the presentation half of a person:
// the display name others read, the locale and timezone their mail is rendered
// in, and the avatar their name appears beside.
//
// Five properties of this schema are load-bearing. Each is a rule the messages
// below are shaped by rather than a convention they happen to follow.
//
// **This is not identity.** Identity answers "who is this" — credentials,
// sessions, lifecycle. Nothing here can authenticate anybody, name an account,
// or move it through its lifecycle, and identity never learns this service
// exists. The two meet only at the pseudonym (ADR-002), which is what stops a
// display name from widening the aggregate that guards authentication — the
// same reasoning ADR-020 used to split `organization` from `workspace`.
//
// **No message here names a subject.** Not one request carries a subject id, a
// user id or an account id. Every RPC acts on the CALLER, whose pseudonym comes
// from the authenticated session and from nowhere else. A field naming the
// person would turn `UpdateProfile` into a way to rename somebody else in front
// of their colleagues, which is impersonation with a REST endpoint in front of
// it.
//
// **An absent field means UNCHANGED, and that is different from CLEARED.**
// Every field of `UpdateProfileRequest` has explicit presence. Leaving one out
// leaves the stored value alone; sending it changes that value. The one field
// that can be emptied — the avatar — says so by being sent as the empty string,
// which is a state the schema can express and an omission is not. The vault
// fields cannot be emptied, and `UpdateProfileRequest` says why at each one.
//
// **No image is ever on this wire.** There is no bytes field in this file and
// there must not be one. `CreateAvatarUpload` hands back a signed POST target
// the browser uploads to DIRECTLY, and `UpdateProfile` then records the object
// key the grant named. An image never enters a request body, an event or a
// database row — see CreateAvatarUploadResponse.
//
// **Nothing personal is stored by anything this file describes except the
// vault.** The display name, the locale and the timezone are personal data
// (internal/platform/pii) and live only in the PII vault, resolved at read
// time. What is projected is the fact that they are set, plus the avatar's
// opaque object key. That is what keeps erasure a key deletion rather than a
// migration (ADR-002, compliance.md §1).
package profilev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/chronos/chronos-go/gen/proto/chronos/profile/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ProfileServiceName is the fully-qualified name of the ProfileService service.
	ProfileServiceName = "chronos.profile.v1.ProfileService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ProfileServiceGetProfileProcedure is the fully-qualified name of the ProfileService's GetProfile
	// RPC.
	ProfileServiceGetProfileProcedure = "/chronos.profile.v1.ProfileService/GetProfile"
	// ProfileServiceUpdateProfileProcedure is the fully-qualified name of the ProfileService's
	// UpdateProfile RPC.
	ProfileServiceUpdateProfileProcedure = "/chronos.profile.v1.ProfileService/UpdateProfile"
	// ProfileServiceCreateAvatarUploadProcedure is the fully-qualified name of the ProfileService's
	// CreateAvatarUpload RPC.
	ProfileServiceCreateAvatarUploadProcedure = "/chronos.profile.v1.ProfileService/CreateAvatarUpload"
)

// ProfileServiceClient is a client for the chronos.profile.v1.ProfileService service.
type ProfileServiceClient interface {
	// GetProfile returns the caller's own profile.
	//
	// The display name, locale and timezone are resolved from the PII vault on
	// every call, and the avatar URL is signed on every call. Neither is cached
	// in a projection: the first because it is personal data, the second because
	// a stored URL outlives its signature.
	GetProfile(context.Context, *connect.Request[v1.GetProfileRequest]) (*connect.Response[v1.GetProfileResponse], error)
	// UpdateProfile changes some of the caller's own profile.
	//
	// It appends ONE `profile.ProfileUpdated.v1` carrying only the fields that
	// actually changed, and it never writes the projection: that table is derived,
	// and a handler writing it would put state in PostgreSQL that no replay
	// reproduces and the next rebuild deletes.
	//
	// Concurrent saves are serialised by the profile stream's own revision, so two
	// browser tabs saving at once produce one ordering rather than a mixture of
	// both. The loser is told `CONFLICT` and retries against the winner's state
	// — it is not silently overwritten.
	UpdateProfile(context.Context, *connect.Request[v1.UpdateProfileRequest]) (*connect.Response[v1.UpdateProfileResponse], error)
	// CreateAvatarUpload mints a signed target the browser uploads an image to
	// directly.
	//
	// A mutation rather than a read, deliberately: it issues a CAPABILITY. The
	// grant is signed, addressed to one key, and usable by whoever holds it until
	// it expires, so it is not something to hand out on a cacheable, retry-happy
	// read — and the idempotency gate makes a double-clicked button reuse one
	// grant instead of littering the bucket with abandoned ones.
	CreateAvatarUpload(context.Context, *connect.Request[v1.CreateAvatarUploadRequest]) (*connect.Response[v1.CreateAvatarUploadResponse], error)
}

// NewProfileServiceClient constructs a client for the chronos.profile.v1.ProfileService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewProfileServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ProfileServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	profileServiceMethods := v1.File_chronos_profile_v1_profile_proto.Services().ByName("ProfileService").Methods()
	return &profileServiceClient{
		getProfile: connect.NewClient[v1.GetProfileRequest, v1.GetProfileResponse](
			httpClient,
			baseURL+ProfileServiceGetProfileProcedure,
			connect.WithSchema(profileServiceMethods.ByName("GetProfile")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		updateProfile: connect.NewClient[v1.UpdateProfileRequest, v1.UpdateProfileResponse](
			httpClient,
			baseURL+ProfileServiceUpdateProfileProcedure,
			connect.WithSchema(profileServiceMethods.ByName("UpdateProfile")),
			connect.WithClientOptions(opts...),
		),
		createAvatarUpload: connect.NewClient[v1.CreateAvatarUploadRequest, v1.CreateAvatarUploadResponse](
			httpClient,
			baseURL+ProfileServiceCreateAvatarUploadProcedure,
			connect.WithSchema(profileServiceMethods.ByName("CreateAvatarUpload")),
			connect.WithClientOptions(opts...),
		),
	}
}

// profileServiceClient implements ProfileServiceClient.
type profileServiceClient struct {
	getProfile         *connect.Client[v1.GetProfileRequest, v1.GetProfileResponse]
	updateProfile      *connect.Client[v1.UpdateProfileRequest, v1.UpdateProfileResponse]
	createAvatarUpload *connect.Client[v1.CreateAvatarUploadRequest, v1.CreateAvatarUploadResponse]
}

// GetProfile calls chronos.profile.v1.ProfileService.GetProfile.
func (c *profileServiceClient) GetProfile(ctx context.Context, req *connect.Request[v1.GetProfileRequest]) (*connect.Response[v1.GetProfileResponse], error) {
	return c.getProfile.CallUnary(ctx, req)
}

// UpdateProfile calls chronos.profile.v1.ProfileService.UpdateProfile.
func (c *profileServiceClient) UpdateProfile(ctx context.Context, req *connect.Request[v1.UpdateProfileRequest]) (*connect.Response[v1.UpdateProfileResponse], error) {
	return c.updateProfile.CallUnary(ctx, req)
}

// CreateAvatarUpload calls chronos.profile.v1.ProfileService.CreateAvatarUpload.
func (c *profileServiceClient) CreateAvatarUpload(ctx context.Context, req *connect.Request[v1.CreateAvatarUploadRequest]) (*connect.Response[v1.CreateAvatarUploadResponse], error) {
	return c.createAvatarUpload.CallUnary(ctx, req)
}

// ProfileServiceHandler is an implementation of the chronos.profile.v1.ProfileService service.
type ProfileServiceHandler interface {
	// GetProfile returns the caller's own profile.
	//
	// The display name, locale and timezone are resolved from the PII vault on
	// every call, and the avatar URL is signed on every call. Neither is cached
	// in a projection: the first because it is personal data, the second because
	// a stored URL outlives its signature.
	GetProfile(context.Context, *connect.Request[v1.GetProfileRequest]) (*connect.Response[v1.GetProfileResponse], error)
	// UpdateProfile changes some of the caller's own profile.
	//
	// It appends ONE `profile.ProfileUpdated.v1` carrying only the fields that
	// actually changed, and it never writes the projection: that table is derived,
	// and a handler writing it would put state in PostgreSQL that no replay
	// reproduces and the next rebuild deletes.
	//
	// Concurrent saves are serialised by the profile stream's own revision, so two
	// browser tabs saving at once produce one ordering rather than a mixture of
	// both. The loser is told `CONFLICT` and retries against the winner's state
	// — it is not silently overwritten.
	UpdateProfile(context.Context, *connect.Request[v1.UpdateProfileRequest]) (*connect.Response[v1.UpdateProfileResponse], error)
	// CreateAvatarUpload mints a signed target the browser uploads an image to
	// directly.
	//
	// A mutation rather than a read, deliberately: it issues a CAPABILITY. The
	// grant is signed, addressed to one key, and usable by whoever holds it until
	// it expires, so it is not something to hand out on a cacheable, retry-happy
	// read — and the idempotency gate makes a double-clicked button reuse one
	// grant instead of littering the bucket with abandoned ones.
	CreateAvatarUpload(context.Context, *connect.Request[v1.CreateAvatarUploadRequest]) (*connect.Response[v1.CreateAvatarUploadResponse], error)
}

// NewProfileServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewProfileServiceHandler(svc ProfileServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	profileServiceMethods := v1.File_chronos_profile_v1_profile_proto.Services().ByName("ProfileService").Methods()
	profileServiceGetProfileHandler := connect.NewUnaryHandler(
		ProfileServiceGetProfileProcedure,
		svc.GetProfile,
		connect.WithSchema(profileServiceMethods.ByName("GetProfile")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	profileServiceUpdateProfileHandler := connect.NewUnaryHandler(
		ProfileServiceUpdateProfileProcedure,
		svc.UpdateProfile,
		connect.WithSchema(profileServiceMethods.ByName("UpdateProfile")),
		connect.WithHandlerOptions(opts...),
	)
	profileServiceCreateAvatarUploadHandler := connect.NewUnaryHandler(
		ProfileServiceCreateAvatarUploadProcedure,
		svc.CreateAvatarUpload,
		connect.WithSchema(profileServiceMethods.ByName("CreateAvatarUpload")),
		connect.WithHandlerOptions(opts...),
	)
	return "/chronos.profile.v1.ProfileService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ProfileServiceGetProfileProcedure:
			profileServiceGetProfileHandler.ServeHTTP(w, r)
		case ProfileServiceUpdateProfileProcedure:
			profileServiceUpdateProfileHandler.ServeHTTP(w, r)
		case ProfileServiceCreateAvatarUploadProcedure:
			profileServiceCreateAvatarUploadHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedProfileServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedProfileServiceHandler struct{}

func (UnimplementedProfileServiceHandler) GetProfile(context.Context, *connect.Request[v1.GetProfileRequest]) (*connect.Response[v1.GetProfileResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.profile.v1.ProfileService.GetProfile is not implemented"))
}

func (UnimplementedProfileServiceHandler) UpdateProfile(context.Context, *connect.Request[v1.UpdateProfileRequest]) (*connect.Response[v1.UpdateProfileResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.profile.v1.ProfileService.UpdateProfile is not implemented"))
}

func (UnimplementedProfileServiceHandler) CreateAvatarUpload(context.Context, *connect.Request[v1.CreateAvatarUploadRequest]) (*connect.Response[v1.CreateAvatarUploadResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chronos.profile.v1.ProfileService.CreateAvatarUpload is not implemented"))
}
