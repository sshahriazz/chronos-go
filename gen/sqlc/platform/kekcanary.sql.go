// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: kekcanary.sql

package platformdb

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const GetKEKCanary = `-- name: GetKEKCanary :one

SELECT kek_name, wrapped FROM kek_canary WHERE id
`

type GetKEKCanaryRow struct {
	KekName string
	Wrapped []byte
}

// Queries for kek_canary.
func (q *Queries) GetKEKCanary(ctx context.Context) (GetKEKCanaryRow, error) {
	row := q.db.QueryRow(ctx, GetKEKCanary)
	var i GetKEKCanaryRow
	err := row.Scan(&i.KekName, &i.Wrapped)
	return i, err
}

const InsertKEKCanary = `-- name: InsertKEKCanary :exec
INSERT INTO kek_canary (kek_name, wrapped) VALUES ($1, $2)
ON CONFLICT (id) DO NOTHING
`

type InsertKEKCanaryParams struct {
	KekName string
	Wrapped []byte
}

// Written once, the first time a build sees an empty table.
//
// DO NOTHING on conflict rather than DO UPDATE: a second writer means two
// processes started against an empty table at once, and the first one's canary
// is as good as the second's. Overwriting would let a process that holds the
// WRONG key replace the proof that it is wrong.
func (q *Queries) InsertKEKCanary(ctx context.Context, arg InsertKEKCanaryParams) error {
	_, err := q.db.Exec(ctx, InsertKEKCanary, arg.KekName, arg.Wrapped)
	return err
}

const TouchKEKCanary = `-- name: TouchKEKCanary :exec
UPDATE kek_canary SET verified_at = $1 WHERE id
`

// Record that the canary verified, so an operator can see the check is running
// rather than assuming it.
func (q *Queries) TouchKEKCanary(ctx context.Context, verifiedAt pgtype.Timestamptz) error {
	_, err := q.db.Exec(ctx, TouchKEKCanary, verifiedAt)
	return err
}
